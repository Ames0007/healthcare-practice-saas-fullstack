# Implementation Status

Authoritative implementation progress tracker for the Healthcare Practice Management SaaS.

Statuses:

```text
NOT_STARTED
IN_PROGRESS
IMPLEMENTED_NOT_VERIFIED
BLOCKED
COMPLETE
```

A task cannot be marked COMPLETE without acceptance-criteria evidence.

| Task | Phase | Status | Commit | Notes |
|------|-------|--------|--------|-------|
| TASK-001 | Phase 0 | COMPLETE | 8d381b6 | Repository bootstrap |
| TASK-002 | Phase 0 | COMPLETE | a227f3b | Backend bootstrap (Laravel 13.26.1 / PHP 8.5.9) |
| TASK-003 | Phase 0 | COMPLETE | c6e656e | Frontend bootstrap (Next.js 16.3.2 / React 19.2.8) |
| TASK-003A | Phase 0 | COMPLETE | 287666b | Graphic charter alignment & visual QA (Spec #10) |
| TASK-004 | Phase 0 | COMPLETE | 59d478a | Local development environment (PostgreSQL 18.6/Redis 8.10.1/MinIO, native/portable) |
| TASK-005 | Phase 0 | COMPLETE | 6a76a05 | PostgreSQL foundation (UUIDv7, test DB, empty migration baseline) |
| TASK-006 | Phase 0 | NOT_STARTED | — | Redis and queue foundation |
| TASK-007 | Phase 0 | NOT_STARTED | — | Testing foundation |
| TASK-008 | Phase 0 | NOT_STARTED | — | Static analysis and linting |
| TASK-009 | Phase 0 | NOT_STARTED | — | CI pipeline |
| TASK-010 | Phase 0 | NOT_STARTED | — | FR/AR localization foundation |
| TASK-011 | Phase 0 | NOT_STARTED | — | Application error contract |
| TASK-012 | Phase 0 | NOT_STARTED | — | Request/correlation IDs |

Foundation Validation Gate must pass before Phase 1 (Identity, Tenancy & Security) begins.

## UI Prototype Sequence

Frontend-only prototype screens built against synthetic mock data, ahead of
backend integration. Separate numbering from TASK-001–TASK-255; does not
imply Phase 0/1 sequencing or backend readiness.

| Task | Status | Commit | Notes |
|------|--------|--------|-------|
| UI-001 | COMPLETE | bb1dec8 | Aujourd'hui dashboard prototype (mock data only, no backend integration) |
| UI-002 | COMPLETE | a655aac | Agenda & appointment prototype: day/week views, drawer, create/edit/reschedule/cancel/no-show, waiting room (mock data only, no backend integration) |
| UI-003A | COMPLETE | ce18978 | Patient list prototype: search, practitioner/next-RDV filters, pagination, Patient 360° route placeholder (mock data only, no creation/editing, no Patient 360°, no backend integration) |
| UI-003B | COMPLETE | 8b679f3 | Create/edit patient prototype: primary+complementary form, responsible practitioner, duplicate detection (phone/name) with Open Existing/Create Anyway, prototype patient-number generation (mock data only, no Patient 360°, no backend integration) |
| UI-004A | COMPLETE | cb34df9 | Patient 360° header + overview: persistent header, 6 real tab routes, Aperçu (4 summary cards + activity timeline), not-found/loading/error states (mock data only, no Dossier Santé/Treatments/Finance detail, no backend integration) |
| UI-004B | COMPLETE | 36d8658 | Patient Rendez-vous tab: upcoming/history appointments derived from Agenda's mock fixtures by patientId, status-group filters, reused AppointmentCard/AppointmentDrawer (extended, not duplicated), + Nouveau RDV/Ouvrir dans l'agenda navigation (mock data only, no Treatments/Finance/Dossier Santé, no backend integration) |
| UI-004C | COMPLETE | 544001f | Patient Traitements/Séances tab: centralized treatment-plan/session fixtures, active/completed treatment presentation, SessionProgress + session tracker, treatment/session detail drawer, Aperçu overview now derives its active-treatment summary from the same fixtures (no contradictory source of truth) (mock data only, no clinical notes/finance/Dossier Santé, no backend integration) |
| UI-004D | COMPLETE | 7becc73 | Patient Factures/Installments tab: centralized invoice/installment fixtures (whole-MAD money, no floats), invoice status registry + installment status registry, financial summary, invoice detail drawer with staged-payment schedule and down-payment representation, PatientHeader balance and Aperçu next-installment now derive from the same fixtures (mock data only, no payment execution/receipts/caisse/accounting, no backend integration) |
| UI-004E | COMPLETE | ed64510 | Patient Paiements/Reçus tab: centralized payment/allocation/receipt fixtures reconciling exactly with UI-004D's own invoice paidAmount/paid-installment history (including one deliberately reversed payment), payment status registry, payment history + summary, Encaisser cash-payment capture prototype (invoice/installment allocation, overpayment/invalid-amount validation, local-session-only state), read-only payment/receipt detail drawer (mock data only, no real payment execution beyond local component state, no caisse/accounting/online-payment, no backend integration) |
| UI-005A | COMPLETE | c26c908 | Dossier Santé — important medical information: centralized MedicalProfile fixtures (kept separate from the administrative Patient type), bounded FR/AR clinical master-data catalog (allergies/history/medications) with case/accent-insensitive search, allergy/history/medication/notes summary cards with a restrained "Important" allergy badge, edit drawer reusing Combobox as a multi-select picker (predefined selection + controlled custom-entry, duplicate prevention), local-session-only edits (mock data only, no consultation history/active consultation/prescriptions/documents, no backend integration) |
| UI-005B | COMPLETE | ebe960d | Dossier Santé — clinical history & consultation timeline: centralized ClinicalEncounter fixtures (consultation/session, cross-referenced with UI-004C's own treatment-session fixture rather than duplicated), purpose-built ClinicalTimeline domain component (distinct from UI-004A's PatientActivityTimeline), newest-first date-grouped history below UI-005A's medical profile, lightweight Tous/Consultations/Séances filter with a filtered-empty state, read-only consultation detail drawer (Motif/Observations/Évaluation/Plan, patient/practitioner/date context, optional associated-appointment link), session cards link to the Traitements/Séances tab instead of a second detail surface (mock data only, no consultation creation/editing, no prescriptions/documents, no backend integration) |
| UI-005C | COMPLETE | 10411c8 | Active Consultation Workspace: dedicated `/app/patients/{id}/consultations/{consultationId}` route (independently addressable, not a Patient 360° tab), centralized ActiveConsultation fixtures (draft + completed) with a pure `toClinicalEncounter` transformation proving completion compatibility with UI-005B's historical shape, structured Motif/Observations/Évaluation/Plan draft form (required-reason validation, local dirty-state tracking) reusing the shared `ConsultationStructuredDetail`/`RelatedAppointmentNote` domain pieces (also refactored into UI-005B's own drawer) for the completed read-only view, ConfirmDialog-gated completion, read-only important-medical-context panel reusing UI-005A's ClinicalSummarySection unmodified (mock data only, no prescriptions/documents/invoice generation/appointment backend transitions, no cross-route Agenda or Clinical History persistence, no backend integration) |
| UI-005D | COMPLETE | 6d554ff | Dossier Santé — Documents & Ordonnances: centralized ClinicalDocument fixtures (bounded analysis/imaging/report/prescription/other category registry with a Lucide icon each, cross-referenced with UI-005B's own ClinicalEncounter fixtures rather than duplicated), lightweight category filter + metadata-only document list, read-only document detail drawer with a prototype-only Télécharger notice, prototype "Ajouter un document" upload (native file input, metadata-only — name/type/MIME/size never file contents — MIME allowlist validation), centralized structured Prescription/PrescriptionItem fixtures with a prototype ORD-2026-#### reference generator, prescription history + read-only detail drawer (shared structured-detail presentation with UI-005C) with an optional "Consultation associée" resolved from UI-005B's fixtures, structured "Nouvelle ordonnance" creation form (multiple medications, add/remove, form-completeness-only validation — no drug/dosage/interaction intelligence anywhere), no document/prescription delete or edit (mock data only, no real file upload/storage, no real PDF generation, no backend integration) |
| UI-006A | COMPLETE | f05b7a8 | Cabinet Finance Dashboard: `/app/finance` replaces the generic "not implemented" placeholder with the first real cabinet-wide financial command center, distinct from Patient 360°'s own Factures/Paiements tabs. New `features/finance/aggregations.ts` derives every KPI from the *existing* UI-004D/E invoice/payment fixtures — `getFinancialSummary`/`getEffectivePaidAmount` reused unmodified rather than re-implemented, so cabinet totals can never independently contradict Patient 360°'s own figures. A small new read-only synthetic `CabinetExpense` fixture set (`components/domain/finance/types.ts`) supports the Décaissements KPI/activity only — no expense-entry UI. Encaissé/À encaisser/En retard/Décaissements/Position caisse KPIs (À encaisser/En retard deliberately NOT period-scoped — current balances, not period activity), Aujourd'hui/Cette semaine/Ce mois period switching (reusing Agenda's own `getWeekStart`), an operationally-ordered (overdue-first) Receivables section navigating to the existing `/app/patients/{id}/invoices` workspace rather than duplicating InvoiceDetailDrawer, and a merged posted-payment/posted-expense "Activité récente" list. Cash position is a documented prototype-only formula (opening 500 MAD, matching Spec #9 Screen 30's own illustrative value, + period collected − period disbursed) with an explicit "not a real Caisse closing" disclaimer. No accounting terminology, no Caisse open/close, no expense entry, no cabinet-level payment capture, no global invoice screen (mock data only, no backend integration) |
| UI-006B | COMPLETE | 5f93ad7 | Global Invoices & Receivables: `/app/finance/invoices` is the cabinet-wide operational invoice workspace UI-006A's own "Voir toutes les factures" now navigates to (real `next/link`, no longer a future-feature Toast). `InvoiceDetailDrawer` (UI-004D) is reused unmodified — proven to never have assumed Patient 360° page composition — plus one small additive `showPatientNavigation` prop (default `false`, zero behavior change for its existing Patient 360° caller) adding "Ouvrir le patient"/"Voir les factures du patient" links for the global context; "Encaisser" still navigates unchanged to the existing `/app/patients/{id}/payments` workflow (UI-004E), so there remains exactly one payment-capture implementation in the whole product. New `features/finance/global-invoices.ts` builds a `GlobalInvoiceRow` read model (cabinet-wide, all patients) from the *existing* `getInvoicesMockData()`/`getPatientsMockData()` fixtures — next-payable-installment derived via `getPayableInstallments` (UI-004E) reused unmodified, operational ordering as one explicit rank per `InvoiceStatus` (overdue → issued → partially_paid → draft → paid → cancelled), a second, deliberately distinct 5-value filter taxonomy from the patient-scoped tab's own 4-value one (documented, not accidental duplication), and case-insensitive search across patient name/number/invoice number. The financial summary (`Total facturé`/`Payé`/`Reste à encaisser`/`En retard`) reuses `getFinancialSummary` (UI-004D) unmodified over the *filtered* result set, per this task's own explicit requirement. Desktop table + mobile card dual-render mirrors `PatientTable`/`PatientCardList`'s exact convention, including the same tablet secondary-column-hiding pattern. No invoice creation/editing/cancellation, no payment capture, no Caisse, no expenses, no accounting terminology (mock data only, no backend integration) |
| UI-006C | COMPLETE | 4cb1a56 | Caisse: Opening & Cash Movements: `/app/finance/caisse` (Spec #2's own IA sitemap nests Caisse under Finance, not a standalone `/app/caisse`) — today's cash register: closed/open states, an opening-balance workflow, and a derived movement history. New `CashSession`/`CashMovement` types added to `components/domain/finance/types.ts` (simplified from Spec #4 §18's `cash_register_sessions`/`cash_movements`, deliberately omitting every closing/reconciliation field — UI-006E's scope). `features/caisse/calculations.ts`'s `buildCashMovements` derives every movement from the *existing* UI-004E payment fixtures (posted, cash-method, matching business date — reversed payments excluded) and UI-006A's expense fixtures (posted, matching date — cancelled excluded), never a second movement universe; deterministic synthetic per-movement times (neither source fixture tracks real time) make ordering reproducible. Theoretical balance (opening + incoming − outgoing) reuses a new `computeCashBalance` primitive extracted from UI-006A's own Position Caisse formula (`features/finance/aggregations.ts`, a pure refactor with zero behavior change, verified by the pre-existing UI-006A test suite passing unmodified) — the two "opening" values are documented as deliberately different semantics (a period-constant projection vs. today's real entered amount). The live route defaults to an already-open synthetic session (openingBalance 500, opened 08:15 by a new synthetic receptionist "Meryem Bakkali" — not "Sara Alaoui", already the canonical empty-fixture patient, to avoid a name collision) while fully supporting a closed/`null` start so the opening workflow itself is demonstrable; opening a second session is structurally impossible once open (the opening form simply stops rendering). A movement row navigates to the existing `/app/patients/{id}/payments` surface, never a duplicate payment detail. "Fermer la caisse" is visible (Spec #9 Screen 30 shows it) but shows only a future-feature notice — no state mutation. No expense entry, no physical cash count, no discrepancy, no real closing, no accounting terminology (mock data only, no backend integration) |
| UI-006D | COMPLETE | fa58f72 | Décaissements & Expenses: `/app/finance/expenses` — today's cabinet cash-expense workspace, scoped to `MOCK_BUSINESS_DATE` like `/app/finance/caisse` itself rather than a broader accounting ledger (Spec #9 Screen 32's own Période/Catégorie filters deliberately not implemented — documented decision). `CabinetExpense` (UI-006A) extended, not replaced, with optional `time`/`createdBy`/`ExpenseSupportingDocument` fields (metadata-only: fileName/mimeType/sizeBytes, never File/Blob/base64/ObjectURL) — UI-006A's original fixtures are unaffected (all new fields optional). New `features/finance/expenses.ts` provides pure, directly-tested derivation: today's-posted filter, newest-first ordering, a strictly-positive `isValidExpenseAmount` (deliberately distinct from Caisse's own `isValidOpeningBalance`, which allows 0), and `createExpenseAndMovement` — a pure builder that returns a matching `CabinetExpense` + `CashMovement` OUT pair by construction (direction/type/expenseId/amount always consistent), applied to local state together in one handler for atomic prototype creation. "+ Nouveau décaissement" (category/amount/description/optional supporting document only — deliberately no beneficiary/payment-method fields, reusing the existing `EXPENSE_CATEGORY_MAP` registry and the same conservative PDF/JPEG/PNG precedent as clinical-document upload, UI-005D) is only reachable while Caisse is open; when closed, creation is hidden and replaced with guidance linking to `/app/finance/caisse` (never a silent auto-open). A dedicated cross-module test proves a newly created expense decreases `features/caisse/calculations.ts`'s own theoretical-balance formula by exactly its amount — the balance itself is never re-rendered on this page (that stays Caisse's own scope). Expense detail is read-only (no edit/delete); "Télécharger le justificatif" reuses UI-005D's exact future-feature notice rather than a new one. No supplier/procurement, no accounting terminology, no Caisse closing/reconciliation/physical count (mock data only, no backend integration) |
| UI-006X | COMPLETE | ba5c4d8 | Finance Workspace Alignment & Navigation: corrective UX/IA task, no new business functionality. New shared `FinanceNav` (`features/finance/components/finance-nav.tsx`) reuses the existing generic `Tabs` primitive (Spec #8 §48, already backing Patient 360°'s own tab bar) rather than a new nav component — Vue d'ensemble/Factures/Caisse/Décaissements, exact/path-aware active-state via `usePathname()` (`/app/finance` only active on the literal route, never every nested one), integrated identically into all four Finance routes. Main sidebar untouched — Finance remains one module (Spec #2's IA sitemap: Échéances/Encaissements have no route yet, so are deliberately not included as tabs, unlike Spec #9 Screen 24's own six-tab wireframe — CLAUDE.md §1, the task's own explicit four-item list). Dashboard recomposed: `KpiSummary` drops the obsolete "Position caisse" card (and its supporting-text "Projection prototype — pas une clôture de caisse réelle." note) entirely — `computeCashPosition`/`OPENING_CASH_POSITION` removed from `features/finance/aggregations.ts` and `cashPosition` from `FinanceKpis`, since nothing renders it anymore; `computeCashBalance` (the true shared primitive) stays, now with its sole remaining caller being Caisse's own `computeTheoreticalBalance`. New `DashboardCaisseSection` replaces it with Caisse's own real operational state — reuses `getDefaultOpenSessionMockData`/`buildCashMovements`/`computeIncomingTotal`/`computeOutgoingTotal`/`computeTheoreticalBalance`/`CaisseSummary` verbatim from `features/caisse/` (UI-006C), never a second cash-position formula; open state shows the real 4-metric summary + opened-at/by + "Voir la caisse"; closed state shows "La caisse n'est pas ouverte." + the same link. `ReceivablesSection` reframed "À encaisser" → "À traiter" with an overdue-vs-to-collect count+total summary line derived from the already-computed `receivables` array (no new total). `RecentActivitySection` rows are now real navigation — payment → `/app/patients/{id}/payments`, expense → `/app/finance/expenses` — mirroring `CaisseMovementList`'s own navigable-row pattern (UI-006C), never a second detail system. `finance-dashboard.tsx`'s own local `BUSINESS_DATE` literal replaced with the canonical `MOCK_BUSINESS_DATE` import (`features/caisse/mock-data.ts`) so the dashboard's Caisse section and Caisse's own session always agree on "today." Invoices/Caisse/Expenses pages otherwise unchanged — only `<FinanceNav />` added, positioned consistently right after `PageHeader`. No new finance domain models, no new business workflows, no Caisse closing, no accounting, no backend/API/persistence (mock data only) |
| UI-006E | COMPLETE | 49c0df8 | Caisse Closing & Reconciliation: completes the daily Caisse lifecycle at `/app/finance/caisse` — physical cash count, expected-vs-actual reconciliation, balanced/discrepancy handling, controlled two-step closing confirmation, and a read-only closed-session summary. `CashSession` (UI-006C) extended, not replaced, with 7 new optional fields (`expectedClosingBalance`/`physicalClosingBalance`/`differenceAmount`/`differenceType`/`discrepancyReason`/`closedAt`/`closedBy`) plus a new `CashDifferenceType` ("balanced"\|"shortage"\|"overage", never accounting debit/credit terms). `CashSessionStatus` stays exactly the two UI-006C values — no third `not_opened` status added: `session === null` still means "not yet opened today," a genuinely completed `status: "closed"` session (with `closedAt` set) is a new, third render branch distinguished by that field's presence, not a new status value (documented decision, §8). New pure `computeCashDifference` (`physicalClosingBalance - expectedClosingBalance`, never reversed) and `resolveCashDifferenceType` in `features/caisse/calculations.ts`, directly tested; physical-count validation reuses `isValidOpeningBalance` as-is (identical whole-MAD/`>=0` rule, not a duplicate function). Two-step closing UI: `CashCountDialog` (read-only opening/incoming/outgoing/théorique recap reusing `CaisseSummary`'s own labels, a physical-count input, live écart via `StatusBadge` — success/danger/warning, never a fully green or red screen — and a reason `Textarea` required only when the difference is non-zero) hands validated results to `CloseConfirmDialog` (reuses the existing `ConfirmDialog` primitive) for an explicit second confirmation before the actual, irreversible mutation — never closes on the first click. New read-only `ClosedCaisseSummary` renders the frozen closing figures (never recomputed after closing, CLAUDE.md §24) alongside live-derived opening/incoming/outgoing (from the same immutable movement history) and the existing `CaisseMovementList`, unchanged. Deterministic `SESSION_CLOSED_AT = "18:35"` (never `Date.now()`); `closedBy` reuses the session's own `openedBy`. No Caisse reopening anywhere, no denomination counting, no accounting terminology, no backend integration (mock data only) |
| UI-007A | COMPLETE | 074ac49 | Équipe Directory & Employee Profiles: `/app/equipe` replaces the generic catch-all placeholder with the first real cabinet Team/HR workspace; nested `/app/equipe/[id]` adds the employee profile. New bounded `TeamMember` domain model (`components/domain/team/types.ts`) deliberately carries no authentication fields (password/MFA/sessions/permissions) — a TeamMember is a person working in the cabinet, not a future auth User (§7); `practitionerId` links to Agenda/Patients/Caisse's own existing lightweight `PRACTITIONERS` fixture (`pr-1`/`pr-2`) for the two members who are also schedulable practitioners, without refactoring any existing selector to consume `TeamMember` (§8, documented future relationship). Centralized `TEAM_ROLE_MAP`/`TEAM_MEMBER_STATUS_MAP` registries (`components/domain/team/`) mirror `expense-category.ts`/`cash-session-status.ts`'s exact pattern — role carries no color/tone (§12: "role is not a status"), status does. 8 centralized synthetic fixtures (`features/team/mock-data.ts`) named to share no first/last-name fragment with any seeded patient (§15) except the two explicitly-named practitioner identities, which deliberately reuse Agenda's own names/ids instead (§16, integrity-tested). `TeamPage` mirrors `PatientsPage`'s exact architecture: search (name/employee number/phone/email, phone normalized via the existing `normalizePhoneDigits`) + role filter (only roles actually represented, §21) + status filter compose and reset together via "Effacer les filtres"; no pagination — 8 synthetic members render fine as one list (§25, documented decision). Desktop table + mobile card dual-render mirrors `PatientTable`/`PatientCardList`'s exact convention, hiding Email/Date d'entrée before Contact/Role/Status/Actions on tablet (§29). `TeamMemberDetailPage` (Spec #9 Screen 34) renders only the "Profil" surface — no tab bar, since Planning/Congés/Paie/Documents/Permissions belong to UI-007B through UI-007F. Bounded `TeamMemberFormDialog` (create/edit, no duplicate detection, no contract/schedule/payroll/document fields) mirrors `PatientFormDialog`'s drawer/validate/submit shape, reusing `isValidEmail`/`isValidMoroccanPhone` unmodified; a generated `EMP-####` reference mirrors `generatePatientNumber`. Edits made from the profile page update only that page's own local state, not `/app/equipe`'s array — the same documented prototype limitation as `PatientDetailPage` (§UI-004A §7). No HR/payroll/attendance/leave/contract/commission functionality, no backend integration (mock data only) |
| UI-007B | COMPLETE | c124394 | Employment Contracts, Roles & Work Schedules: evolves UI-007A's single-tab employee profile into an Employee 360° workspace — `TeamMemberNav` (Profil/Contrat/Planning, mirrors `PatientDetailPage`'s own explicit-`activeTab`-prop `Tabs` usage rather than `FinanceNav`'s `usePathname`-prefix pattern, since this nav is nested under a per-member `[id]`) at nested `/app/equipe/[id]/contract` and `/app/equipe/[id]/schedule`. `TeamMember` itself is untouched (§10) — two new, deliberately separate domain types instead: `EmploymentContract` (`components/domain/team/types.ts`) with a bounded `ContractType` ("permanent"/"fixed_term"/"part_time"/"internship"/"other" — the domain spec only defines a free-text `employees.employment_type` column, so the task's own explicit list is authoritative; French labels "CDI"/"CDD" are the standard Francophone terms for those two shapes, not an invented Moroccan legal category) and `ContractStatus` ("active"/"ended" only), each with its own registry (`contract-type.ts`, `contract-status.ts`) mirroring `expense-category.ts`/`cash-session-status.ts`'s exact pattern. No remuneration field anywhere (§20) — grep- and test-confirmed absent. `WorkInterval` (mirrors Spec #4 §20.1 `employee_work_schedules` 1:1 — one row per interval, several rows sharing a `weekday` model a split shift, satisfying "multiple work intervals per day" without a list field) is deliberately not coupled to Agenda's date-based `formatWeekdayShort` (would require a fake "reference week" just to borrow labels) — a small standalone `team.weekday.*` translation set instead. 7 centralized contract fixtures (`mock-contracts-data.ts`) cover all four required scenarios (active open-ended/active fixed-end/ended historical/no contract at all — the last reusing UI-007A's own "deliberately unlinked" team-7 rather than a new fixture); 6 members' worth of work-interval fixtures (`mock-schedule-data.ts`) — two split-shift (lunch-break) weeks demonstrating multiple intervals per day, four single-interval weeks — each member's own `computeWeeklyScheduledHours` matches their own contract's `weeklyHours` exactly (integrity-tested). Contract edit is edit-only (no "create a new contract" flow — task scopes this to editing the current one); work-schedule edit is a bounded per-weekday editor (worked/rest + up to 2 intervals, non-overlap validated) that always replaces a member's *entire* interval set on submit, never a per-interval CRUD surface. All edits remain page-local state only (same documented prototype limitation as UI-007A's own profile edit and `PatientDetailPage`, UI-004A §7). No attendance/check-in-out/lateness/overtime/leave/payroll/salary/commission/document-storage/auth functionality anywhere, no backend integration (mock data only) |
| UI-007CDEF | COMPLETE | 6ed65fd | Complete HR Operations Prototype — Attendance, Leave, Payroll & Practitioner Commissions: completes the Employee 360° workspace (Profil/Contrat/Planning/Présence/Congés/Paie, +Commissions for practitioners) across four internal gates, all sharing one coherent fixture universe — no contradictory duplicate mock data anywhere. **Gate 1 — Attendance** (`components/domain/team/types.ts`'s `AttendanceRecord`/`AttendanceStatus`, deliberately a frontend-only prototype ahead of the approved backend scope — see `docs/implementation/DECISIONS.md` ADR-005, since Spec #4 §20 and Spec #3 WF-36 both say clock-in/out is not a required V1 backend entity/workflow): only raw `checkIn?`/`checkOut?` are stored, every status/worked/late/early-departure/overtime figure is *derived* by `features/team/attendance.ts`'s pure functions against the member's own real `WorkInterval`s (never a second hardcoded schedule). `computeWorkedMinutes` subtracts only the unpaid gap *between* a split shift's own two intervals from the raw check-in/check-out span — never naively "final minus first" — so a split-shift day's worked time is never overstated. Cabinet workspace at `/app/equipe/attendance` (reached from the Équipe directory header, not a new sidebar entry) correctly shows a rest day for the whole cabinet on the live default (`MOCK_BUSINESS_DATE` is a Sunday) — honest behavior, not an omission; a `businessDate` seam demonstrates every real state. Per-employee Présence tab adds deterministic check-in/check-out actions (`MOCK_NOW_TIME`, never `Date.now()`) plus a restrained recent history. **Gate 2 — Leave** (`LeaveRequest`/`LeaveBalance`/`LeaveType`/`LeaveRequestStatus`): bounded leave-type vocabulary (annual/sick/unpaid/other, no invented Moroccan statutory categories), inclusive-calendar-day duration (`computeLeaveDurationDays`, matching the task's own "26-28 août = 3 jours" example exactly), combined per-employee request list + Approve/Reject (via a `LeaveDecisionDialog` wrapping the existing `ConfirmDialog`, reason required only to reject). Approving moves the request's own duration from `available` to `used` on its balance; a pending or rejected request touches neither. `doesApprovedLeaveCoverDate` is the Gate 1↔2 integration point — only an *approved* request re-labels an otherwise-unexplained day as "En congé"; a pending one never does. **Gate 3 — Payroll** (`PayrollPeriod`/`PayrollEntry`/`PayrollAdjustment`, an explicitly cabinet-operational prototype, never a statutory Moroccan tax/CNSS/AMO/IR engine, §37/§77): `baseAmount` is a payroll-specific synthetic figure, never retroactively added to `EmploymentContract` (which UI-007B deliberately kept salary-free); `overtimeMinutes` is duration-only and reconciles exactly against Gate 1's own real attendance overtime for the same member/period (`computePeriodOvertimeMinutes`, integrity-tested) — no monetary overtime rate is invented, so overtime never contributes to gross pay. Bonuses/deductions are bounded, addable only while the period is `draft` (a `finalized` period is read-only, no edit/delete of any kind). A read-only `PayslipDialog`'s own "Télécharger" action shows the exact future-feature notice this task specifies, never a real PDF. Meryem Bakkali's July entry reproduces the task's own §47 worked example verbatim (5 000 + 300 bonus = 5 300 net). **Gate 4 — Commissions** (`CommissionRule`, basis fixed to `"collected_payments"` — the only basis the approved specifications actually demonstrate, Spec #9 Screen 38 / Spec #3 WF-40): eligible activity is derived live from the *existing* `getPaymentsMockData()`/`getInvoicesMockData()` fixtures via `practitionerId` → the real Agenda `PRACTITIONERS` fixture → `Invoice.practitionerName` — never an independent commission-revenue number. Eligibility requires both `role === "practitioner"` AND a real `practitionerId` (Othmane Zouiten, a practitioner with no `practitionerId`, deliberately proves the distinction — no Commissions tab, and direct route access shows a not-applicable state). Only patient identity/date/service/amount are ever shown — no clinical data. Dr. Benali's payroll `commissionAmount` for every period is proven, by a dedicated reconciliation test, to equal Gate 4's own live `computeCommissionAmount` output — never a second hardcoded figure. A `cross-hr-integrity.test.ts` walks the full required chain (Contract → Schedule → Attendance → Payroll overtime, Leave → Attendance, Finance activity → Commission → Payroll) end to end for one real member. 143 net new tests (849 total, up from the UI-007B baseline of 706), typecheck/lint/build clean, backend regression unaffected (10/10, no backend files touched) |
| UI-008ABCD | COMPLETE | 76b505c | Pharmacie & Stock — Catalog, Stock Parameters, Lots & Expiration, Movements & Alerts: replaces the generic Stock placeholder with `/app/stock` (Vue d'ensemble/Articles/Mouvements/Lots & expirations, `StockNav` mirroring `FinanceNav`'s `usePathname`-prefix pattern), sidebar label renamed "Stock" → "Pharmacie & Stock" (`nav.stock`). New bounded domain (`components/domain/stock/types.ts`): `InventoryItem`/`InventoryLot`/`StockMovement` — deliberately **no stored balance field anywhere**; every item/lot balance is derived live from `StockMovement[]` (`computeItemStockBalance`/`computeLotBalance`), the same "derive, don't duplicate" discipline as Caisse's expected balance and Attendance's worked minutes, proven by `cross-inventory-integrity.test.ts` (a lot-tracked item's own lot-balance sum always equals its item-level total). A single 10-value `category` taxonomy doubles as the item-type axis — a second, near-1:1 parallel taxonomy was judged unnecessary duplication per the task's own §14 caution. `expirationTracking` is only ever `true` alongside `lotTracking` (`isValidItemTrackingFlags`) since expiration dates live on lots, not items (Spec #2 §37). **Gate 1 — Catalog & Stock Parameters**: `StockPolicy` layers optional safety-stock/reorder-point/maximum/reorder-quantity/lead-time planning metadata on top of the approved-schema's mandatory `minimumStock` (Spec #4 §23.1) — a deliberate, non-persisted enrichment, not a contradiction (see `docs/implementation/DECISIONS.md` ADR-006). `resolveStockAttentionStatus` reproduces the task's own worked example exactly (Compresses stériles: 18/25/low, `item-02`). Articles catalog (`/app/stock/items`) — search/category/status filters, desktop table + mobile cards (mirrors `GlobalInvoiceTable`/`CardList`) — plus a bounded Add-article dialog; editing an existing article lives on its own Item 360° detail page (`/app/stock/items/[id]`, mirrors `TeamMemberDetailPage`'s header-then-tabs-then-switched-content shell) instead of the list, mirroring Team's own edit-on-detail convention. **Gate 2 — Lots & Expiration**: `resolveLotExpiryStatus` (expired/expiring_soon/valid) uses a documented 30-day warning-horizon default (Spec #3's own open-questions list leaves this unresolved — ADR-006) shared by every consumer via one `EXPIRY_WARNING_HORIZON_DAYS` constant. Read-only Lots & Expirations workspace (`/app/stock/lots`, cabinet-wide, worst-expiry-first) plus each item's own Lots tab; lots are never created here — only through Stock IN (Gate 3). An expired lot that still holds remaining quantity (`item-13`'s `lot-13-1`) is deliberately kept visible even though its item overall looks healthy — a real WF-48 scenario a stock-level alert alone would never surface. **Gate 3 — Stock Movements**: one shared `StockMovementFormDialog` for Stock IN/OUT/Adjustment (bounded reason vocabulary per type, `REASON_OPTIONS_BY_MOVEMENT_TYPE`); negative stock is disallowed outright (`wouldCauseNegativeItemBalance`/`wouldCauseNegativeLotBalance`, ADR-006) — an OUT exceeding the item's or the selected lot's own real balance is blocked before submit, never silently clamped. A Stock IN on a lot-tracked item can create a brand-new lot inline (number + expiration if tracked) or add to an existing one; a Stock OUT/Adjustment must select an existing lot with remaining balance, never an empty one. Movement history shows a running balance (`buildMovementHistory`, chronological replay reversed for display) reconciling exactly with the item's own live balance. The cabinet-wide `/app/stock/movements` workspace starts with an article selector, then reuses the exact same `ItemMovementsContent` component as the item's own Mouvements tab — no duplicate movement-table implementation. **Gate 4 — Dashboard & Alerts**: `/app/stock` shows the three Spec #2 §42.5 KPIs exactly (low-stock items, expiring lots, 30-day movement volume) plus worst-first attention lists for stock and expiry, every figure reconciling exactly with the same pure functions Gates 1-3 already built (`cross-inventory-integrity.test.ts`) — never an independently hardcoded dashboard number. 24 centralized synthetic items across all 10 categories/lots/movements form one coherent fixture universe spanning every gate (no contradictory duplicate mock data), covering every `StockAttentionStatus`, every `LotExpiryStatus`, every `StockMovementReason`, and both `adjustment` directions at least once. No procurement/purchase-order vocabulary anywhere (Spec #7 §24), no pricing/cost field (this domain is quantity-based only), no LocalStorage/global persistence, no API calls, no authentication. 161 net new tests (1010 total, up from the UI-007CDEF baseline of 849), typecheck/lint/build clean, backend regression unaffected (10/10, no backend files touched) |
| UI-009ABC | COMPLETE | 16d0873 | Communication Center — Message History, Templates & Automation Rules, Communication Dashboard: replaces the generic Communication placeholder with `/app/communication` (Vue d'ensemble/Messages/Modèles/Automatisations, `CommunicationNav` mirroring `StockNav`'s exact one-tab-per-gate growth and `usePathname`-prefix pattern); `nav.communication` already read "Communication," no rename needed. New bounded domain (`components/domain/communication/types.ts`): `CommunicationMessage` mirrors Spec #4 §24.2's `communication_messages` row field-for-field (including its `queued/sent/delivered/failed` status ENUM verbatim, not the task's own hedged "pending" suggestion) and deliberately carries **no `direction` field** — every message is outbound by construction (ADR-007). Every `patientId`/`appointmentId`/`invoiceId`/`installmentId` is a soft reference resolved at render time against the *existing* Patients/Agenda/Invoices fixtures, never a duplicate universe (CLAUDE.md §12) — `appointmentId` is only ever paired with a `patientId` in `{pat-1..pat-5}`, the sole range where Patients' and Agenda's two independently-seeded fixture sets agree on identity (a pre-existing, documented split). **Gate 1 — Message History**: `CommunicationPurpose` (11 values, the union of Spec #2 §39.1's template categories) drives the history table's Type column and doubles as `MessageTemplate.purpose` in Gate 2 — one shared vocabulary, never two. Messages workspace (`/app/communication/messages`) — search (patient name/number/recipient phone, reusing `normalizePhoneDigits`) + Canal/Statut filters exactly matching Spec #9 Screen 41's own wireframe (no invented Type filter) — opens a read-only detail drawer (`Dialog variant="drawer"`, mirrors `PaymentDetailDrawer`) with patient/appointment/invoice cross-links into the *existing* Patient 360°/appointments/invoices surfaces, never a duplicate view. 14 centralized synthetic messages cover every status/channel/purpose category the task names, cross-linked to real `pat-1`/`pat-4`/`pat-9` invoices (`inv-1`/`inv-2`/`inv-3`) and real `apt-2`/`apt-4`/`apt-5`/`apt-6`/`apt-7`/`apt-14` appointments. **Gate 2 — Templates & Automations**: `renderTemplate` (`features/communication/templates.ts`) is a pure, `eval`-free, `dangerouslySetInnerHTML`-free substitution function over a strict 10-key variable allowlist (`CommunicationVariableKey`, the union of the task's own list and Spec #2 §39.2's) — a known token missing its context value renders the deterministic placeholder "—", never a blank string that could misread as real content; an unrecognized `{{token}}` is left untouched, never interpreted. `MessageTemplate.variables` is *derived* from `body` via `extractVariablesFromBody`, never independently authored, so a template's declared variables can never drift from what its body actually references. Templates workspace (`/app/communication/templates`) reproduces Spec #9 Screen 42's editor exactly (Nom/Canal/Langue/Message/VARIABLES reference list/live APERÇU against a fixed sample context) plus a card-based list (Name/Channel/Locale/Active, [Modifier]); the edit dialog is `key`-ed by template id so switching the edit target between rows always remounts with fresh form state. `AutomationRule` (7 fixed canonical `CommunicationEventType`s, one per Spec #2 §40 V1 rule) exposes only an active/inactive toggle — the literal, bounded reading of §40's own closing line ("Owner can configure whether each automation is active"), not a rule builder (CLAUDE.md §3). 10 centralized template fixtures include an Arabic one (`tpl-3`, reproducing Screen 42's own "تذكير بالموعد" example) and a deliberately inactive one (`tpl-8`, whose sole referencing rule is also inactive — proven coherent). **Gate 3 — Dashboard & Operational Actions**: `/app/communication` shows three KPIs (failed/queued/7-day-volume — `MESSAGE_VOLUME_WINDOW_DAYS`, an explicit shorter-than-Stock default since messaging cadence is daily not weekly, ADR-007) plus Failed/Pending attention sections, every figure reconciling exactly with `cross-communication-integrity.test.ts`. Retry (now operational on both the dashboard's own inline button and the Gate 1 drawer's previously-dormant one) re-queues a failed message and clears its failure metadata — it never fabricates a "sent"/"delivered" outcome, since no real provider acknowledgment exists in this prototype (ADR-007). The bounded Send Message dialog composes exactly one message to one existing patient via a searchable `Combobox`; selecting an active template renders its body against the *real* selected patient's own identity/practitioner fields (appointment/invoice-specific tokens correctly render "—", since this compose flow has no appointment/invoice picker) and records a synchronous local `"sent"` message — never `"delivered"`. No real WhatsApp/SMS provider, no Meta/Twilio integration, no message delivery network calls, no queues, no webhooks, no AI-generated messaging, no marketing/bulk sends, no LocalStorage/global persistence, no API calls. 134 net new tests (1144 total, up from the UI-008ABCD baseline of 1010), typecheck/lint/build clean, backend regression unaffected (10/10, no backend files touched) |
| UI-010ABC | COMPLETE | cec4cae | Reports & Cabinet Configuration — Reports, Cabinet Settings, Services & Operational Configuration: replaces the generic Rapports/Paramètres placeholders with `/app/rapports` (Vue d'ensemble/Activité/Finance/Équipe/Stock, `ReportsNav` mirroring `StockNav`'s exact pattern) and `/app/parametres` (Cabinet/Services & tarifs/Horaires/Numérotation, `ParametresNav`). **Reports MUST derive from existing domain fixtures** (task's own explicit rule) — every KPI is computed by a pure function reusing an *already-shipped* module's own calculations (`computeFinanceKpis` UI-006A, `buildItemRows`/`getExpiryAttentionLots` UI-008ABCD, `computeAttendance`/`computePeriodOvertimeMinutes` UI-007CDEF), never a second `mock-report-data.ts` fixture universe; proven end to end by `cross-reporting-integrity.test.ts`. **Gate 1 — Reports**: Overview reproduces the task's own exact 4-block/3-metric wireframe (Activité: Rendez-vous/Patients vus/Taux d'absence; Finance: Encaissé/À encaisser/En retard; Équipe: Heures travaillées/Retards/Heures supplémentaires; Stock: Articles en rupture/Stock faible/Lots proches expiration), reusing Finance's own `PeriodSelector`/`getPeriodRange`/`FinancePeriod` (UI-006A) rather than a second period-toggle. `StockReportKpis` deliberately splits Stock's own combined `lowStockItemsCount` into `outOfStockCount`/`lowStockCount` per the wireframe's own two-number ask — a different grouping of the exact same `StockAttentionStatus` rows, never a new balance derivation (proven reconciling `outOfStockCount + lowStockCount === computeStockKpis().lowStockItemsCount`). Activité/Finance detail pages add a bounded "Performance par praticien" table (Spec #3 WF-72) and a "Répartition par statut" breakdown — deliberately **no invented "confirmation rate"** formula, since Spec #2 §42.1 names that KPI with no defined formula anywhere in the approved specifications; "Revenue by service" (§42.2) is deliberately omitted too, since `Invoice` carries no service/service-id field and a free-text-description join would be an unreliable guess, not a derivation. `APPOINTMENT_STATUS_ORDER` added to the existing `appointment-status.ts` map (additive, no other module touched). 60 targeted tests. **Gate 2 — Cabinet Settings**: New bounded domain (`components/domain/settings/types.ts`) — `CabinetProfile` (Spec #4 §5.1 `tenants`, narrowed: no `slug`, no `logo_file_id` — logo is a read-only placeholder, no fake upload flow) with `currencyCode`/`timezone` fixed and non-editable (Spec #2 §44: "Currency fixed to MAD initially"; no timezone picker UX is defined anywhere). `/app/parametres` is a single-record view/edit toggle (not a list+dialog, since there is exactly one cabinet profile) — reuses the existing `isValidEmail`/`isValidMoroccanPhone` validators (UI-003B) rather than a second pattern. Edits are local component state only; saving does **not** propagate to `topbar.practiceName` elsewhere — no global store exists, the same local-state-only boundary every prior UI-00X edit flow already has. 16 targeted tests. **Gate 3 — Operational Configuration**: `CabinetService.name` seeds verbatim from Agenda's own pre-existing `SERVICES` string array (task §5: "provide them a coherent configuration home") enriched with duration/price/`schedulingMode` (reusing `AppointmentSchedulingType` verbatim, not a second exact/window enum); `/app/parametres/services` reuses `TemplateFormDialog`'s own two-instance-dialog-plus-`key`-remount pattern for Add/Edit (UI-009ABC's documented stale-`useState` fix, applied preemptively). `CabinetWorkingHoursDay` is deliberately **cabinet-level only, not per-practitioner** — Spec #4 §12 only ever defines `practitioner_working_hours` (no `practice_working_hours` table exists), and Spec #2 §46's own prose distinguishes "Practice hours" from "Practitioner hours" without resolving how a multi-practitioner cabinet reconciles the two (a genuine, recorded specification gap); `weekday` reuses Team's own `Weekday`/`WEEKDAY_ORDER` verbatim, never a second weekday sequence. `/app/parametres/numerotation` is deliberately **read-only**: concurrency-safe sequence allocation ("lock sequence row during allocation", Spec #4 §59) is a backend/database concern this frontend prototype does not implement; PAT/EMP rows reuse `generatePatientNumber`/`generateEmployeeNumber` verbatim, FAC/REC apply the identical regex-extract-max+1 pattern locally (no existing generator to reuse for those two). Appointment operational parameters (buffer time, booking-advance window, cancellation policy) are deliberately **not implemented** — grep confirmed the approved specifications define none of these beyond what Services & Pricing's own `durationMinutes`/`schedulingMode` already cover (CLAUDE.md §3/§50). 42 targeted tests. **FINAL**: `cross-reporting-integrity.test.ts` + `cross-configuration-integrity.test.ts` (21 combined reconciliation tests) prove every Reports/Settings figure traces back to its own source module. No Laravel integration, no API calls, no database changes, no persistence, no accounting engine, no payment gateway, no production document generation, no fake file-upload flow. 123 net new tests (1267 total, up from the UI-009ABC baseline of 1144), typecheck/lint/build clean, backend regression unaffected (10/10, 26 assertions, no backend files touched) |
| UI-010BC | COMPLETE | c9ef37f | Cabinet Settings & Operational Configuration — Rendez-vous, Paiements, Documents: completes the Paramètres IA list (Cabinet/Services/Horaires/Rendez-vous/Paiements/Numérotation/Documents, `ParametresNav` extended from 4 to 7 tabs in that order) left open by UI-010ABC. **Gate 1 — Cabinet Settings**: verification-only, no code change. Spec #4 §5.1 (`tenants`) and Spec #2 §44 define only a single `address`/`city` pair — no `legalName`/split-address/`postalCode`/`country`/structured `logoMetadata` field exists anywhere in the approved domain model, so `CabinetProfile`/`CabinetSettingsPage` (UI-010ABC) already satisfy the spec exactly; the task's own conceptual field list is illustrative, not literal, per its own "Use specification fields where different... Do not model backend tenant/security settings" (ADR-009 §1). **Gate 2 — Operational Configuration** (Services/Horaires/Numérotation preserved unchanged from UI-010ABC): new `AppointmentSettings`/`PaymentMethodRow`/`DocumentSettings` domain types (`components/domain/settings/types.ts`). Rendez-vous (`/app/parametres/rendez-vous`) is a single-record view/edit toggle bounded to `defaultSchedulingMode`/`defaultDurationMinutes` — the only two concerns Spec #2 §46 names for this section; no public-booking toggle, since `/book` remains a documented visual placeholder with no real request flow (ADR-009 §3). Paiements (`/app/parametres/paiements`) is a **read-only** one-row table reusing Finance's own `PaymentMethod` type verbatim (`"cash"` only, per that type's own doc comment citing CLAUDE.md §23) rather than an editable card/virement/chèque toggle list Finance cannot actually process (ADR-009 §2). Documents (`/app/parametres/documents`) is a single-record view/edit toggle for footer text/header note/document language — deliberately omits invoice/prescription template selection (no real document-rendering system exists, task's own "NO production document generation") and tax display (`Invoice` has no tax field at all); `footerText`/`documentLanguage` derive by default from the same Cabinet profile fixture (`buildDefaultDocumentFooter`), never an independently invented example (ADR-009 §4). `cross-configuration-integrity.test.ts` extended with 3 new reconciliation checks (Rendez-vous default mode matches Services' own majority mode; Paiements' row matches Finance's own payment fixtures' `method`; Documents' footer derives live from the Cabinet profile fixture). 33 net new tests (1300 total, up from the UI-010ABC baseline of 1267), typecheck/lint/build clean, backend regression unaffected (10/10, 26 assertions, no backend files touched) |
| UI-011ABC | COMPLETE | 3335a92 | Subscription Lifecycle, Plans/Entitlements & Referral Program: replaces the generic catch-all placeholder at `/app/abonnement` (already a real sidebar entry, `nav-config.ts`) with a full SaaS-commercial module — Abonnement/Plans/Parrainage, `SubscriptionNav` (not added to the global sidebar, task's own explicit rule). New domain layers `components/domain/subscription/` and `components/domain/referral/` (two separate SaaS-domain concepts, task §34) — deliberately separate from `domain/settings/` (CabinetSettings is operational configuration; Subscription is the platform-tenant commercial relationship, task §8). Every commercial value traces to the approved specifications or is deliberately left undefined rather than invented (ADR-010): 2 plans only (Solo/Cabinet — no invented "Cabinet+"), Cabinet's 3-practitioner/5-staff limits reproduce Spec #9 Screen 47's own worked example verbatim, `PlanPrice.amount` is `undefined` everywhere (Spec #2 §50 explicitly defers pricing), `storage_bytes` carries no limit (Screen 47's own literal "..."), boolean entitlements are uniformly enabled (no existing module is actually plan-gated in this prototype). **Gate 1 — Subscription Lifecycle**: all 6 approved statuses implemented (`trialing/active/expired/grace/blackout/cancelled`, CLAUDE.md §11/Spec #4 §28.4 verbatim) via one `buildSubscriptionFixture` base + `addDaysIso` offsets (never independently typed dates) — the default Active/Cabinet fixture reproduces Screen 47's exact renewal date "23 septembre 2026" (exactly one month after `MOCK_BUSINESS_DATE`). Grace period is exactly 3 days (Spec #2 §49.3/WF-55, `GRACE_PERIOD_DAYS`); "expiring soon" threshold reuses the spec's own D-15 reminder point (`EXPIRING_SOON_THRESHOLD_DAYS`) rather than inventing a separate number. No trial *duration* is ever stated — only a real `trialEndsAt` date and live-computed days remaining (task's own explicit "do not hardcode 14 days" rule). Blackout renders Screen 49's own full takeover (no `PageHeader`/nav/Usage/History) as a page-scoped presentational state only — the global `AppShell` sidebar is untouched (ADR-010 §6, task §9/§31: frontend entitlement UX is never authoritative). "Renouveler" opens a purely informational `ConfirmDialog` (the task's own suggested copy verbatim) — never mutates `Subscription`, never simulates a payment. Usage rows (Praticiens/Personnel) are *derived live* from Équipe's own real `TeamMember` fixtures (`countActivePractitioners`/`countActiveStaff`) — 2 active practitioners, 4 active staff — never independently hardcoded (task §27). History is bounded to trial-started/activated/renewed/plan-changed/referral-reward-applied — `subscription_payments` (Spec #4 §28.5) deliberately excluded (ADR-010 §5). **Gate 2 — Plans & Entitlements**: centralized `hasEntitlement`/`getEntitlementLimit`/`getUsageState` resolvers (`features/subscription/entitlements.ts`, Spec #5 §39) — no component compares a plan code/name string directly anywhere in this module. `/app/abonnement/plans` is a restrained 2-column comparison (price/practitioners/staff/storage/3 boolean entitlements); "Choisir ce plan" opens the same informational dialog as Gate 1's "Renouveler" (no checkout, no local plan mutation). `EntitlementLimitNotice` (the task's own §31 Feature-Lock UX pattern) has a genuine, non-fabricated case to render: Solo's real 1-practitioner limit against the real fixture's 2 active practitioners blocks that selection with an explanatory message — WF-74's own "limit reached" scenario, using real derived data rather than a manufactured demo state. **Gate 3 — Referral**: `Referral`/`ReferralReward` (Spec #4 §29, all 6 `ReferralStatus` values implemented) — `reward_type` has exactly one approved value anywhere in the specs (`free_subscription_time`), `rewardMonths` is always `1` (Spec #2 §51.2/WF-59's own "+1 free subscription month," never a cash/discount reward). `/app/abonnement/parrainage` reproduces Screen 50's own layout — code/link derived deterministically from the Cabinet profile fixture (`buildReferralCode`/`buildReferralLink`, format `app.ma/r/{code}` verbatim from the wireframe, never a real production domain), Copy action uses the real Clipboard API with a `Toast` confirmation, no tracking network call. Referral rows show a real `StatusBadge` for one of the 6 domain statuses plus, only for a `qualified` referral with a matching *applied* reward, a composed "+1 mois" — never an invented 7th status label. `cross-subscription-integrity.test.ts` (6 tests) proves the full chain: Subscription→Plan→Entitlements→Usage reconciles, and Referral→Qualification→Reward→Subscription-benefit reconciles (the history event's `months` and date always equal the real `ReferralReward`'s own fields, never independently duplicated, task §41). No Stripe/payment-gateway integration, no real checkout, no Laravel/API calls, no database changes, no LocalStorage/global persistence, no real subscription enforcement, no coupon engine, no affiliate payout system. 92 net new tests (1392 total, up from the UI-010BC baseline of 1300), typecheck/lint/build clean, backend regression unaffected (10/10, 26 assertions, no backend files touched) |
| UI-011X | COMPLETE | 82e1542 | Access Governance, Roles, Permissions & Delegation of Authority: adds "Accès & permissions" as `ParametresNav`'s 8th tab, `/app/parametres/access` (Utilisateurs/Rôles/Permissions/Délégations/Historique via its own nested `AccessGovernanceNav`, never added to the global sidebar). New domain layer `components/domain/access/` — deliberately distinct from `domain/team/` `TeamMember` (a person employed by the cabinet, may exist with no `UserAccount`), `domain/subscription/` `PlanEntitlement` (tenant-purchased capability, never per-user), and any real authentication (no password/MFA/session fields anywhere). The 16-key permission-catalog core is copied verbatim from two independently-confirming sources — Spec #4 §4.3 `membership_permissions` and CLAUDE.md §9's identical list — rather than the task's own deeper suggested scheme (ADR-011 §1); Screen 35's own single "CAISSE [x] Accéder" checkbox confirms the coarser grain directly. **Gate 1 — Roles & Permissions**: `PERMISSION_CATALOG` (23 keys across 14 domains, task §8's own domain list), `PermissionSensitivity` (UI-warning-only, never enforcement, task §7) and exactly the 3 V1 conceptual roles Spec #2 §29.1 defines (Owner/Admin holds every permission; Practitioner defaults to agenda+own clinical+admin-patient-view+reports; Receptionist/Staff defaults to admin-patient+appointments only — "clinical access should not be casually enabled," §58). `/app/parametres/access/roles` is a stacked, editable domain-grouped checklist per role (Screen 35's own layout); `/app/parametres/access/permissions` is the read-only catalog reference. 33 targeted tests. **Gate 2 — User Access**: `UserAccount`/`TenantMembership` (Spec #4 §4.1-4.2, no password/session fields) link to 5 of 8 real Équipe `TeamMember` fixtures — 3 deliberately have no `UserAccount` at all, demonstrating the module's own explicit rule. Meryem Bakkali's membership reproduces Spec #9 Screen 35's worked example exactly (role defaults + 2 individual grants + 1 individual restriction). `computeEffectivePermissions` (built here as the Gate 3/4 prerequisite, mirroring UI-011ABC's own "resolver built early, surface later" precedent) resolves Role ∪ Grants ∪ Delegations minus Restrictions, restrictions always winning. `/app/parametres/access` (Utilisateurs, the module's own root even though built after Rôles/Permissions — task's nav order, not build order) reproduces Screen 46's own Nom/Profil/Statut/Accès columns ("Complet"/"Personnalisé"/role-name, never a 4th invented label); "Gérer les accès" opens a drawer with one unified per-permission toggle (never two independent grant/restrict checkboxes, ADR-011 §4) proven to maintain a real invariant: restrictions only ever name a role-granted key. 37 targeted tests. **Gate 3 — Delegation**: has zero backing in the approved specifications (grep-confirmed across all 10 spec files) — implemented per the task's own explicit Gate 3 instructions (CLAUDE.md §1: task instructions outrank specs), kept minimal (one permission per delegation, task's own objective diagram) and grounded in the closest approved precedent, Spec #4 §7.3's dormant `patient_access_grants` (ADR-011 §2). `resolveDelegationStatus` derives all 4 states (`scheduled/active/expired/revoked`) live from dates — revocation always wins even mid-window. `caisse.manage`/`subscription.manage`/all three `access.*` permissions are `delegatable: false` (physical-custody, WF-74, and privilege-escalation reasoning respectively, ADR-011 §3). `validateDelegationForm` enforces both the catalog's `delegatable` flag and that the delegator *currently* effectively holds the permission (`computeEffectivePermissions`) — you cannot delegate authority you do not have. `/app/parametres/access/delegations` — Create (Combobox delegator/delegate, Select bounded to delegatable permissions, date range) and revoke (sets `revokedAt`, never deletes). 28 targeted tests. **Gate 4 — Effective Access & Audit**: `EffectiveAccessSummary` — a compact, at-a-glance "what does this person actually have, and why" list inside the same drawer, reading the identical `computeEffectivePermissions` result the checklist already shows permission-by-permission (never a second computation, task §22). `AccessAuditEvent` (task §23) is a bounded, static, read-only fixture — every event traces to a fact the current fixture state still holds (Meryem's own grant/restriction events name exactly her real membership's current keys; the delegation-linked events reference real delegation ids whose own `createdAt`/`revokedAt` match exactly) — `subscription_payments`-style fabrication never occurs here either. `/app/parametres/access/historique` — newest-first, read-only. `cross-governance-integrity.test.ts` (6 tests) proves the full chain end to end: every permission/role/membership id referenced anywhere resolves to a real entity, Meryem's full effective-access set reconciles exactly (role minus restriction plus 2 grants plus 1 active delegation), and no delegation ever targets a non-delegatable permission. 26 targeted tests. No Laravel authorization, no authentication implementation, no security claims, no database, no API, no real enforcement across existing modules, no LocalStorage/global persistence. 124 net new tests (1516 total, up from the UI-011ABC baseline of 1392), typecheck/lint/build clean, backend regression unaffected (10/10, 26 assertions, no backend files touched) |
| UI-011X-FIX | COMPLETE | 3ce7843 | Restore Paramètres navigation hierarchy for Access Governance — regression repair, no new functionality. Root cause: all 5 Access feature pages (`users-page.tsx`/`roles-page.tsx`/`permissions-page.tsx`/`delegations-page.tsx`/`historique-page.tsx`) rendered only `AccessGovernanceNav`, never `ParametresNav`; `ParametresNav` itself already had its 8th "Accès & permissions" tab correctly wired since UI-011X — only the Access pages' own render tree omitted it. Fix: import and render `ParametresNav` immediately above `AccessGovernanceNav` in each of the 5 pages, matching the codebase's existing inline-nav-per-page convention (no `layout.tsx` nesting precedent exists anywhere below the app shell, so none was introduced). Every `/app/parametres/access*` route now renders the full three-level hierarchy (main sidebar → `ParametresNav` with "Accès & permissions" active → `AccessGovernanceNav` with its own active tab); the 7 pre-existing Settings routes are unaffected (still show `ParametresNav` alone, `AccessGovernanceNav` never appears there). New `parametres-access-hierarchy.test.tsx` (22 targeted regression tests) proves both nav levels render together on all 5 Access routes, `ParametresNav`'s 7 pre-existing hrefs are unchanged, `AccessGovernanceNav` never leaks onto a non-Access Settings route, active-state resolves correctly at both levels simultaneously, and the hierarchy holds under Arabic/RTL. 22 net new tests (1538 total, up from the UI-011X baseline of 1516), typecheck/lint/build clean, backend regression unaffected (10/10, 26 assertions, no backend files touched) |
| UI-FIX | COMPLETE | cccaf4a | Dead Buttons & Interactive Actions Audit — the reported dead topbar "Créer" button plus a codebase-wide sweep for similar silently-inert controls (static scan of every `<Button`/`<button` JSX tag missing `onClick`/`type="submit"`/`htmlFor`, across `frontend/src`). **Créer** (`AppTopbar`) had no `onClick`/`href` at all; now opens `QuickCreateDialog` (reuses the existing `Dialog` primitive, `variant="modal"`) listing 5 of the specs' 6 recommended quick-create actions — Rendez-vous/Patient/Mouvement de stock/Message/Décaissement, each a pure navigation `Link` into that action's own already-built creation workflow (`/app/agenda`, `/app/patients`, `/app/stock/movements`, `/app/communication`, `/app/finance/expenses`), never a duplicate form. "Nouvelle facture"/"Nouvel encaissement" deliberately omitted — no manual invoice-creation workflow exists anywhere (`GlobalInvoicesPage`'s own doc comment), and payment capture is hard-scoped to a specific patient+invoice with no safe cabinet-wide entry point to deep-link into (ADR-012 §2-3). Audit found 4 more dead/mis-disabled controls, all fixed by reusing the codebase's own established future-feature `Toast` convention (already used pervasively for PDF/print/receipt downloads) rather than staying silent: the topbar notification bell and user-account button, `MobileNav`'s "Plus" (still a non-navigating placeholder — distinct from Créer, never repurposed), and `SubscriptionPage`'s Blackout "Contacter le support"/"Se déconnecter" (previously hard-`disabled` with no explanation, contradicting CLAUDE.md §11's "support/logout... remains accessible" — now active). `AppShell` now owns the Quick Create dialog's open state and the shared Toast message, passed to `AppTopbar`/`MobileNav` as props — one shared instance, not one per surface. Also fixed: Aujourd'hui's "Ouvrir" (`NextAppointmentSection`) had zero `onClick`/`href` — now a real `Link` to `/app/agenda` (not a specific appointment: `TodayAppointment.id` is a reduced fixture id with no reliable join to a real Agenda appointment id, confirmed by comparing both fixture sets directly). Paramètres/Access Governance navigation hierarchy (UI-011X-FIX) verified unchanged. No Laravel/API calls, no LocalStorage/persistence, no new subsystem (no Notification Center, User Menu, or secondary-module sheet built). 10 net new tests (1548 total, up from the UI-011X-FIX baseline of 1538), typecheck/lint/build clean, backend regression unaffected (10/10, 26 assertions, no backend files touched) |
| UI-AGENDA-X | COMPLETE | 04ec5a9 | Dynamic Cabinet Calendar & Availability Exceptions — additive to the existing UI-010ABC weekly `CabinetWorkingHoursDay` (unchanged, still `/app/parametres/horaires`, "Horaires habituels"). New `CabinetCalendarException` (`components/domain/settings/types.ts`) — zero backing in the approved specifications (grep-confirmed, mirrors Delegation's own ADR-011 §2 precedent), grounded in the closest real one, Spec #4 §12.3's practitioner-scoped `availability_exceptions`, extended to 5 cabinet-scoped types the task's own §5 names explicitly: `public_holiday`/`exceptional_closure`/`rest_day`/`modified_hours`/`exceptional_opening` (registry: `calendar-exception-type.ts`). No separate `allDay` field — closed-ness is always derived from `type`, never an independently-settable flag that could contradict `intervals` (ADR-013 §2). New `/app/parametres/horaires/exceptions` route (`HorairesNav`, a real URL-addressable 2-tab sub-nav nested beneath `ParametresNav`, mirroring `AccessGovernanceNav` exactly — both navs render on every Horaires route, directly applying the UI-011X-FIX lesson). `resolveEffectiveCabinetAvailability` is the single centralized pure resolver every consumer reads (Add/Edit dialog's NORMAL/EXCEPTION preview, conflict detection) — an active exception always *replaces* the weekly schedule outright, never unions with it (task §7); at most one active exception per date (`hasActiveExceptionForDate`, enforced by `validateCalendarExceptionForm`), editing replaces in place. A date strictly before `MOCK_BUSINESS_DATE` is past, read-only history (no Modifier/Supprimer renders); the business date itself remains editable. `findConflictingAppointments` is real, never fabricated — filters Agenda's own actual `getAgendaMockAppointments()` fixtures by date/non-terminal-status against the resolved effective intervals, never mutates a single appointment; every conflict-relevant fixture reconciles exactly with real Agenda ids (`cross-calendar-exceptions-integrity.test.ts`: the exceptional-closure fixture conflicts with exactly real `apt-14`, the modified-hours fixture with exactly real `apt-12`+`apt-13`, both derived — never invented counts like the task's own illustrative "4 rendez-vous"). Both `public_holiday` fixtures are real Moroccan national dates (Fête du Trône, Marche Verte) correctly ordered past/future around `MOCK_BUSINESS_DATE`. The optional Agenda banner (task §26) was deliberately NOT implemented: the task's own text is conditional on spec support ("If the specifications support it"), which grep-confirmed does not exist — correctly out of scope, not a gap (ADR-013 §7). No Public Booking, no Laravel/API calls, no database, no LocalStorage/persistence. 60 net new tests (1608 total, up from the UI-FIX baseline of 1548), typecheck/lint/build clean, backend regression unaffected (10/10, 26 assertions, no backend files touched) |
| UI-003C | COMPLETE | a6353e0 | Patient CIN & Social Coverage — additive extension of `PatientFormDialog`'s existing complementary-info section (UI-003B), from a task-supplied wireframe. `Patient`/`PatientFormValues` (`features/patients/types.ts`) gain `cin` (optional, format-unvalidated string — mirrors `city`'s own no-validation precedent, CLAUDE.md §3) and `isSociallyCovered`/`insuranceRegime` (new `InsuranceRegime` 7-value union: `amo_cnss`/`amo_cnops`/`amo_tns`/`amo_tadamon`/`amo_achamil`/`amo_students`/`other`). Zero specification backing (grep-confirmed across all 10 spec files) — implemented as a single administrative identifier field, not the "Insurance/AMO workflows" CLAUDE.md §50 excludes from V1 (no claims, no reimbursement, no insurer integration); see ADR-014 for the scope boundary reasoning. New "Couverture sociale" sub-card (mirrors the existing "Contact d'urgence" sub-card exactly) reuses the established radio-group pattern from Agenda's `SchedulingFields`/`CancelConfirmDialog` (`type="radio"`, shared `useId()` name) for the Oui/Non toggle; the régime `Select` only renders, and is only required, while `isSociallyCovered` is `true` — switching back to "Non" clears any already-picked régime before submit, so a stale régime can never persist against an uncovered patient (ADR-014 §3). Both fields follow the same edit-form-only precedent as `birthDate`/`email`/`city`/`address` — never surfaced read-only in Patient 360°'s header or overview cards, which stay unchanged. The wireframe's own "Sexe" field was deliberately not added — it was not marked as new in the source wireframe and does not exist anywhere in the current codebase (ADR-014 §4). No Laravel/API calls, no database, no persistence (mock data only, 2 of 16 patient fixtures seeded for realism, mirroring `birthDate`'s own precedent). 4 net new tests (1612 total, up from the UI-AGENDA-X baseline of 1608) plus 1 existing AR/RTL test extended, typecheck/lint clean, backend regression unaffected (10/10, 26 assertions, no backend files touched) |
| UI-LEAVE-X | COMPLETE | e129cb5 | Cabinet Leave Agenda — adds "Agenda des congés" at `/app/equipe/leave-calendar`, a static route sibling of `/app/equipe/[id]` (mirrors `team-attendance-page.tsx`'s exact precedent), reached from the Équipe directory header alongside the existing "Présence du jour" link — no main-sidebar change. The per-employee Congés tab (`TeamMemberLeaveContent`, UI-007CDEF) is entirely unchanged and remains the sole create/approve/reject surface. Purely a read projection over the existing `LeaveRequest[]`/`TeamMember[]` — `features/team/leave-calendar.ts`'s `LeaveCalendarEvent` is a derived view model (`buildLeaveCalendarEvents`), never a second authoritative record, consumed identically by new Month/Week/List views (`components/leave-calendar-{month,week,list}-view.tsx`) and a read-only `leave-event-drawer.tsx` whose only action ("Voir la demande") links to the existing per-employee workflow, never a second editor. A multi-day request correctly repeats across every date it spans (`doesEventCoverDate`); `getApprovedTeamMembersAway`/`countApprovedPractitionersAway` only ever count `status === "approved"` — pending never counts as confirmed absence, rejected is hidden by the Status filter's restrained "Approuvé + En attente" default but remains explicitly reachable. Practitioner-overlap derives from `TeamRole === "practitioner"`, deliberately not the narrower Agenda `practitionerId` link Commission eligibility (Gate 4) requires (ADR-015 §2) — an inactive or not-yet-linked practitioner still counts. `mock-leave-data.ts`'s existing single fixture array gains `lr-5`/`lr-6` (Amal Idrissi + Hamza Rifai, both real team members, approved and overlapping on 2026-08-27) — the one real scenario the prior 4 fixtures never demonstrated: two people simultaneously on approved leave, one a practitioner; every pre-existing fixture and the team-4 empty-state guarantee are byte-for-byte unchanged (ADR-015 §1). Dashboard metrics are always whole-cabinet and business-date-anchored, never scoped to the currently browsed period (ADR-015 §4). Cabinet closure context (the task's own explicitly optional integration) reuses `resolveEffectiveCabinetAvailability` outright — a real `CabinetCalendarException` closure shows a distinct "Cabinet fermé" badge, never converted into a per-employee leave request; an ordinary non-working weekday never triggers it (ADR-015 §5); adjacent-month overflow cells in Month view show only the dimmed day number, never event/closure content, avoiding a real ambiguity a leading-week `public_holiday` fixture would otherwise have caused. Routing regression (`team-routing.test.tsx`) proves `/app/equipe/leave-calendar`/`/app/equipe/attendance` never collide with the dynamic `[id]` route — no real TeamMember id equals either string, and `TeamMemberDetailPage` degrades to its own not-found state even in the theoretical collision case. No automatic leave approval/rejection, no payroll changes, no Laravel/API calls, no database, no LocalStorage/persistence. 65 net new tests (1677 total, up from the UI-003C baseline of 1612), typecheck/lint clean, backend regression unaffected (10/10, 26 assertions, no backend files touched) |
| UI-DOCS-X | COMPLETE | 078a06a | Platform Document Generation, Preview, Download & Print — activates real client-side PDF generation for Invoice, Receipt, Prescription and Payslip, superseding the earlier prototype's "no PDF generation" instruction for these four completed workflows only. New shared `features/documents/` architecture (ADR-016): `types.ts`'s `GeneratedDocumentBase` (task §4's own suggested shape), one pure `buildXDocument()` projection per type (`invoice-document.ts`/`receipt-document.ts`/`prescription-document.ts`/`payslip-document.ts` — every amount/date read directly off the existing `Invoice`/`Payment`+`Receipt`/`Prescription`/`PayrollEntry`, never recalculated with a second formula), one `<XDocumentPdf>` render component per type sharing `pdf-shell.tsx`'s cabinet-identity header/footer chrome and `pdf-styles.ts`'s tokens (same hex palette as `design-system/tokens.css`), and a single `download.ts` (`generateDocumentBlob`/`triggerBlobDownload`/`triggerBlobPrint`) covering all four — no per-feature duplication of the generate/download/print mechanics. PDF technology: `@react-pdf/renderer` 4.9.0 (justified in ADR-016 — structured-primitive rendering, not HTML rasterization; no other realistic option offers real custom-font Arabic glyph embedding). The four existing detail drawers/dialogs (`invoice-detail-drawer.tsx`, `payment-detail-drawer.tsx`, `prescription-detail-drawer.tsx`, `payslip-dialog.tsx`) already serve as the Preview surface (task §9) — no separate "Aperçu" dialog was added; "Télécharger PDF"/"Imprimer" now generate and act on a real PDF instead of a future-feature toast. Filenames follow the task's own exact examples (`Facture-FAC-2026-00143.pdf`, `Recu-REC-2026-00383.pdf`, `Ordonnance-PAT-00281-2026-08-29.pdf`, `Bulletin-Paie-EMP-0003-2026-08.pdf`), sanitized via `filename.ts`, never an internal database id. Real end-to-end generation (not just data-model tests) is proven by `pdf-generation.test.ts` via `renderToBuffer`, and the resulting files were visually inspected through an independent renderer (poppler `pdftoppm`) — French documents render correctly, professionally, with money/date/reconciliation all correct on-page. **Arabic document generation is deliberately gated OFF, not shipped**: real visual QA of the rendered PDFs found `@react-pdf/renderer`'s Arabic shaping pipeline drops/corrupts individual glyphs, reproduced across two different embedded fonts and even after pre-shaping into Arabic Presentation Forms — the task's own explicit §31 STOP condition. `isDocumentLanguageSupported` (`capabilities.ts`) blocks generation and shows a real translated notice instead whenever `documentLanguage === "ar"` (default is French; only reachable if a cabinet explicitly switches Document language in Paramètres → Documents) — recorded as RISK-015, full detail in ADR-016. `DocumentDetailDrawer` (clinical uploaded documents) and `ExpenseDetailDrawer` (expense justificatifs) are deliberately left unchanged — they represent Type-B *uploaded* attachments with no real file bytes anywhere in this prototype's fixtures, so no real download is possible without inventing file content; both already give explicit future-feature feedback (never a silent dead button, §37). Reports/Caisse export controls do not exist in the current UI (grep-confirmed) — nothing to activate there, none added. No backend document storage, no electronic signature, no fiscal certification, no provider APIs, no Laravel/API calls, no database, no persistence beyond the browser's own download. 38 net new tests (1715 total, up from the UI-LEAVE-X baseline of 1677), typecheck/lint/build clean, backend regression unaffected (10/10, 26 assertions, no backend files touched) |
| UI-013ABCDE | COMPLETE | e82cc4b | SaaS Platform Administration — a new `/admin/*` Platform Admin console, a genuinely separate product surface from `/app/*` (cabinet) and `/book` (public patient), never confused with either (task's own "CRITICAL DOMAIN DISTINCTION"). **Gate 1 — Admin Shell & Dashboard**: `app/admin/layout.tsx` replaces TASK-003's static placeholder with a real 5-item nav (`lib/admin-nav-config.ts`: Vue d'ensemble/Cabinets/Abonnements/Utilisateurs/Activité) — never reuses `AppShell`/`AppSidebar` (task §5), never a dark admin theme (Spec #10 §32). No authentication gates `/admin/*` (task §6, RISK-018) — intentional, deferred to a future task. `AdminDashboardPage` derives every KPI (Cabinets actifs/en essai/restreints, Abonnements actifs/à renouveler/expirés, Utilisateurs total/actifs) and the attention queue purely from the same `Tenant[]`/`Subscription[]`/`PlatformUser[]` arrays every other admin screen reads (`tenants.ts`/`subscriptions.ts`/`platform-users.ts`/`attention.ts`) — "À renouveler" reuses UI-011ABC's own `isExpiringSoon`/D-15 threshold rather than a second invented number; "Restreints" unions `tenant.status === "suspended"` with `subscription.status === "blackout"`, counting a tenant once even when both hold (Spec #4 §57.7). **Gate 2 — Tenants**: `Tenant` (`components/domain/platform-admin/types.ts`) is a wholly new type — no `Tenant` entity existed anywhere in this codebase before (`CabinetProfile` models only the single prototype tenant, has no `id`/`status`). 7 fixture tenants (`mock-tenants-data.ts`) cover all 3 `TenantStatus` and all 6 `SubscriptionStatus` values at least once and all 7 CLAUDE.md specialties; `tenant-1`'s name/specialty read directly from the real `CabinetProfile`, and its subscription is the exact same object `/app/abonnement` itself reads (`getSubscriptionMockData()`) — never a duplicate. `/admin/tenants` (`TenantsPage`) reproduces wireframe Screen 55's own column layout (Cabinet/Owner/Specialty/Plan/Status/Users) with search (name/owner substring) and plan/status filters (`filterTenantDirectoryRows`). Tenant 360° (`/admin/tenants/[id]`) is a local JS-only tablist (`role="tablist"`, not the shared href-based `Tabs` — task §9 rules out per-tab routes) with Aperçu/Abonnement/Utilisateurs/Historique panels; bounded tenant-status actions (`getAvailableTenantActions`/`applyTenantAction` — Suspend from active, Reactivate from suspended, nothing from closed/terminal, task §1: "NO destructive tenant deletion") require a reason (`ConfirmDialog`+`Textarea`, Spec #2 §55.2 "controlled and audited") and update local state only (task §1: "NO real tenant suspension"). **Gate 3 — Subscriptions**: the other 6 subscriptions (`mock-platform-subscriptions-data.ts`) reuse UI-011ABC's own status-variant builders (`getTrialingSubscriptionMockData()`, etc.) with only `id`/`tenantId` overridden — every internal date relationship those builders already prove consistent (grace = expiry + `GRACE_PERIOD_DAYS`) is inherited, never hand-recomputed. `/admin/subscriptions` (`SubscriptionsPage`) lists every platform subscription with status/plan filtering; row actions link straight to Tenant 360° — no `/admin/subscriptions/[id]` route exists (task §9), subscription detail and bounded actions (`getAvailableSubscriptionActions`/`applySubscriptionAction`: manual renewal, forced blackout only from grace, cancellation) live on Tenant 360°'s own Abonnement tab, which also surfaces the real `PlanEntitlement` rows for the tenant's plan reusing `/app/abonnement`'s own exact translation keys — never a new entitlement-label namespace. **Gate 4 — Users**: no spec/wireframe/task/i18n precedent names a standalone "platform user administration" screen (grep-confirmed across all 10 specs and `06-master-implementation-plan.md`) — the only grounding is Spec #4 §55's bare `platform_admin_users` table name with zero fields, which actually names the SaaS operator's own login identity (task §6's deferred authentication concern), not a directory to build here. Recorded decision (ADR-018 §1): Gate 4 instead directories Spec #4 §4.1 `users`/§4.2 `tenant_memberships`, genuinely spanning multiple tenants (unlike UI-011X's own `TenantMembership`, narrowed by its own doc comment to one synthetic tenant). `tenant-1`'s 5 platform users are *derived* from the real Access Governance fixtures (`mapAccessUsersToPlatformUsers`, mapping `role-owner-admin`/`role-practitioner`/`role-receptionist` to Spec #4 §4.2's own `profile_type` ENUM) rather than re-authored — Othmane Zouiten (`user-5`) stays `disabled` at the platform level too. The other 6 tenants get genuinely new fixtures deliberately covering all 4 `UserAccountStatus` values (an `invited` owner who never logged in, a `locked` owner, a `disabled` owner) `tenant-1`'s own real data alone doesn't exercise. `/admin/users` (`UsersPage`) lists every platform user with their tenant relationships (task §23); `PlatformUserDrawer` (mirrors `UserAccessDrawer`'s established pattern, no `/admin/users/[id]` route per task §9) shows full membership detail and bounded status actions (`getAvailableUserActions`/`applyUserAction`: Disable from active, Reactivate from disabled, Unlock from locked, nothing from invited). **Gate 5 — Operations**: `/admin/activity` (`ActivityPage`) covers the audit log (`mock-platform-audit-data.ts` — 5 static events, every one traceable to a real fixture fact; `paudit-1` mirrors Access Governance's own real `audit-7` Othmane-deactivation event exactly, same date, proven by `cross-platform-admin-integrity.test.ts`) and the attention queue (only non-zero rows, never a wall of zeroes). A dedicated support/tenant-context workspace and impersonation were deliberately not built — both are marked conditional-future by the specifications themselves (Spec #1 §27, Spec #2 §55.6 "Future/controlled"), not merely unspecified (ADR-018 §5). None of Gate 2-4's bounded actions propagate across pages or to this feed — a deliberate, documented session-local boundary (ADR-018 §3, RISK-017), not an oversight. FR/AR: full parity (2025 keys each, verified), RTL proven across every page. No Laravel integration, no API calls, no database changes, no real tenant suspension/impersonation/subscription mutation, no payment gateway, no authentication, no LocalStorage/persistence. Admin nav trimmed from TASK-003's 8 items to exactly the 5 this task's own scope covers (Plans/Master Data/Referrals admin screens remain a real, spec-grounded gap for a future task, ADR-018 §2). 109 net new tests (66 targeted Gate 1-5 engine/integrity tests across `tenants.test.ts`/`subscriptions.test.ts`/`platform-users.test.ts`/`attention.test.ts`/`audit.test.ts`/`cross-platform-admin-integrity.test.ts`, plus 43 component tests across `admin-dashboard-page.test.tsx`/`tenants-page.test.tsx`/`tenant-detail-page.test.tsx`/`subscriptions-page.test.tsx`/`users-page.test.tsx`/`activity-page.test.tsx`) — 1913 total, up from the UI-012ABCDE baseline of 1804, typecheck/lint/build clean, backend regression unaffected (10/10, 26 assertions, no backend files touched) | Public Booking & Effective Availability — replaces the `/book` visual placeholder with a real, accountless public booking journey built entirely on existing operational configuration/scheduling data, no second fixture universe anywhere (task §5/§77). **Gate 1 — Availability Engine** (`features/booking/availability.ts`, pure, React-free): `getBookableServices` (active `CabinetService` only — no per-service practitioner-eligibility field exists anywhere in the domain, so every schedulable practitioner is eligible for every active service, documented in the function's own comment); `getSchedulablePractitioners` reuses the canonical `TeamMember.practitionerId` → `AgendaPractitioner` relationship exactly (role `"practitioner"` AND `status === "active"` AND a real `practitionerId` link — Othmane Zouiten, `team-7`, a practitioner with neither, is integrity-tested absent). `getDayAvailability` resolves, in order: past date → `resolveEffectiveCabinetAvailability` (reused outright from UI-AGENDA-X, never re-implemented) → approved leave (`doesApprovedLeaveCoverDate`, reused outright from UI-007CDEF — pending/rejected never block) → practitioner `WorkInterval` intersection (`intersectIntervals`, the one centralized interval-intersection helper) → duration-fitting slot generation (`generateCandidateSlots`, each effective interval walked independently so a slot can never bridge a split-hours/lunch closure) → existing-appointment occupancy (`computeOccupiedIntervals`/`isSlotFree`, reusing `toRange`/`overlaps`/`TERMINAL_STATUSES` newly exported from `features/agenda/conflict.ts` — an additive `export` on 3 already-shipped pure functions, zero behavior change — rather than a third re-implementation of the same overlap math). Slot-step granularity (30 minutes, `BOOKING_SLOT_STEP_MINUTES`) is a recorded architectural decision (ADR-017) — no spec or `AppointmentSettings` field defines one, so it reuses the one real existing precedent (`day-view.tsx`'s own `SLOT_MINUTES = 30`, `conflict.ts`'s own `suggestAlternativeTimes` stepping), never invented arbitrarily. No booking horizon and no minimum-notice rule are implemented — neither is defined anywhere in `AppointmentSettings` or the approved specifications (task §18/§19); the calendar's own month-navigation UI is separately capped 3 months forward as a documented UI-layer convenience, never an engine business rule (ADR-017). Six machine-readable `UnavailableReason`s collapse to 4 public-safe labels via `getUnavailableReasonLabelKey` (`features/booking/labels.ts`) — `practitioner_on_leave` and `practitioner_not_scheduled` deliberately render the *exact same* generic "Indisponible" as each other, proven by a dedicated privacy test (task §38/§66: a patient must never be able to infer a practitioner is on leave). 45 targeted Gate 1 tests (41 in `availability.test.ts`, 4 in `labels.test.ts`) plus `cross-booking-integrity.test.ts` (10 reconciliation tests against the real fixtures — including the real `modified_hours` exception on 2026-08-24 that already coincides with real appointments `apt-12`/`apt-13`, and Dr. Amal's real approved leave 2026-08-26–28). **Gate 2/3 — Public Shell, Service/Practitioner/Date/Slot Selection**: `/book` (existing canonical public route, `/book/{cabinet-slug}` routing is an explicitly out-of-scope backend/multi-tenant concern per `CabinetProfile`'s own doc comment) keeps its existing account-less public layout (widened `max-w-sm` → `max-w-xl` to fit a month calendar + multi-column slot grid, task §73's "do not stretch patient forms" still respected), replacing the placeholder `BookPage`/`FoundationBadge` outright — `FoundationBadge` is explicitly documented as "never used on real product screens," and `/book` is now one. A 5-step flow (Service → Praticien → Date & heure → Vos informations → Confirmation, `BookingProgress`) is a deliberate, documented extension of Spec #9 Screen 51's older single-form wireframe (no explicit practitioner field) — justified because this task's own explicit GATE 2 instructions (§13-17/§30-32, mandatory practitioner-selection step) outrank a specification wireframe predating any real availability engine, per CLAUDE.md §1's own priority order; recorded as part of ADR-017, not a silent deviation. `AvailabilityCalendar` is a purpose-built month grid (never `<input type=date>`, which has no per-day availability to visualize) — unavailable days are disabled, struck through, and carry a safe `aria-label` reason, never color-only (task §33). **Gate 4 — Patient Details, Review & Booking**: the contact form is bounded to Spec #9 Screen 51's own exact field list (Prénom/Nom/Téléphone/Commentaire) — no CIN, no social coverage, no clinical data, integrity-tested absent (task §42/§43); phone reuses `isValidMoroccanPhone` unmodified. `bookingReducer` (`booking-state.ts`) invalidates a later step's selection only when an earlier one actually *changes* (re-confirming the same service via "Modifier" never wipes an already-picked date/slot). Submission re-validates the exact selected slot against live sources immediately before creating the local record (task §47) — a stale slot returns the user to the date step with `slotNoLongerAvailable`, never a silent failure. The local booking record reuses the canonical `AgendaAppointment` shape outright (status always `"requested"`, matching Spec #9 Screen 52/WF-04 §5 exactly) with a synthetic `public-*` `patientId` — never one of the real `PATIENTS` fixture ids, never a probabilistic match (task §45). A deterministic `DEM-{date}-{seq}` reference (`buildBookingReference`) is an explicitly recorded prototype-local convention, distinct from the backend's own `NumberingSequenceRow` registry (which covers PAT/EMP/FAC/REC only — appointments were never part of it). **Gate 5 — Confirmation & Session Integrity**: confirmation wording matches Spec #9 Screen 52 verbatim ("Le cabinet doit encore confirmer votre rendez-vous... Vous recevrez une confirmation par WhatsApp ou SMS") — never claims a confirmed appointment, never claims a message was already sent. Every confirmed booking accumulates in `sessionBookings` (component state only, task §52: no `localStorage`, a refresh resets the wizard) and is merged into the availability engine's own appointment source for the rest of the session — so a just-booked slot cannot be immediately double-booked locally (task §51), without ever mutating Agenda's own real appointment array. "Prendre un autre rendez-vous" resets the wizard but deliberately preserves `sessionBookings`/the reference sequence. Privacy (task §65/§66, integrity-tested): no existing appointment's real patient name, no leave reason, no internal closure reason, no employee private data is ever rendered on `/book`. 16 component tests (`public-booking-page.test.tsx`, React Testing Library) drive the full golden path (FR and AR) end to end through real DOM interaction, plus calendar/privacy/invalidation/re-booking-prevention checks — including a `booking-state.test.ts` suite (18 tests) for the reducer/local-record builders. No Laravel integration, no API calls, no database changes, no real SMS/WhatsApp sending, no payment gateway, no CAPTCHA, no calendar-provider integration, no real persistence; production concurrency safety is explicitly documented as a future backend responsibility, never claimed here. 89 net new tests (1804 total, up from the UI-DOCS-X baseline of 1715), typecheck/lint/build clean, backend regression unaffected (10/10, 26 assertions, no backend files touched) |
| UI-013X | COMPLETE | 7a6fc37 | Authentication UX & Cabinet Onboarding — replaces the last two TASK-003 Foundation/Demo placeholders (`/auth`, `/onboarding`) with production-shaped frontend UX. **Gate 1 — Authentication** (`features/auth/`): Login (`/auth`, existing route), Forgot password (`/auth/forgot-password`, new), Reset password (`/auth/reset-password`, new) — explicitly NOT real authentication (task's own "Authentication UX ≠ real authentication"). Grep across all 10 specs found zero mentions of a demo-credential/prototype-identity mechanism, and the one analogous precedent (`/admin/*`'s own no-auth boundary, UI-013ABCDE ADR-018) is "render without a gate," not "fake a login" — so per task §8's own fallback, a valid Login submission never sets a session/cookie/LocalStorage entry/JWT; `validateLoginForm` passes, then a bounded Toast ("L'authentification réelle sera disponible après l'intégration du serveur.") reuses the exact established "future-feature Toast" convention (UI-FIX's dead-button audit) — the form always validates and always gives visible feedback, never silently doing nothing (ADR-019 §1). New `PasswordInput` (`components/password-input.tsx`) show/hide field — no password-visibility pattern existed anywhere in this codebase before (grep-confirmed) — mirrors `Input`'s exact label/error/describedby structure. `ForgotPasswordPage`'s success state is shown identically for any well-formed email, real or not, never disclosing account existence (CLAUDE.md §17's own patient-existence-disclosure principle applied to accounts); no email is actually sent. `ResetPasswordPage` renders and validates unconditionally regardless of any `token` query string — there is no backend to verify one against, so no fake verification result is ever produced (task §10). `validateLoginForm`/`validateForgotPasswordForm`/`validateResetPasswordForm` (`validation.ts`) reuse `isValidEmail` (`features/patients/patient-form-validation.ts`) outright; no password-policy rule is invented anywhere (task §7/§10) — only required-field and must-match checks. **Gate 2 — Cabinet Onboarding** (`features/onboarding/`): a 6-step wizard — Cabinet → Horaires → Services → Équipe → Préférences → Récapitulatif → Terminé — composing EXISTING Paramètres form-value types/validators/components outright (task §14): `CabinetProfileFormValues`+`validateCabinetSettingsForm`+`CABINET_SPECIALTY_MAP` (Cabinet step), `CabinetWorkingHoursFormValues`+`isValidWorkingHoursForm` (Horaires step, itself reusing Équipe's own `isValidWorkInterval`), the literal `ServiceTable`/`ServiceFormDialog` components (Services step — the exact same components/translation keys Paramètres → Services & tarifs renders), `AppointmentSettingsFormValues`+`validateAppointmentSettingsForm` (Préférences step) — there is no `OnboardingCabinet`/`OnboardingService`/`OnboardingWorkingHours` type anywhere, proven by `cross-onboarding-integrity.test.ts` (6 reconciliation tests, including one proving an onboarding-drafted service's `svc-N` id would never collide with a Paramètres-created one). Step sequence (ADR-019 §2) reconciles two sources rather than picking one wholesale: Spec #7 §28/wireframe Screens 03-07 define a 5-screen sequence (Spécialité → Cabinet → Horaires → Services → Complete, Horaires *before* Services, no Équipe/Préférences step) — Horaires-before-Services is honored per task §15's own "follow exact specifications if defined," Spécialité is folded into the Cabinet step's field list (matching this task's own explicit §17 field list) rather than kept as a separate screen, and Équipe/Préférences/a rich Récapitulatif are kept as this task's own explicit, repeated Gate 2 instructions, which the specs are silent on rather than contradicting. No minimum-one-active-service requirement exists anywhere in the approved specifications (grep-confirmed) — Continue from Services is never blocked on an empty list (task §19). The Équipe step is explicitly optional/non-blocking (Spec #2 §6.6's own framing) and captures only a bounded `OnboardingDraftTeamMember` (first/last name, professional title, role, phone, email) — never a full `TeamMemberFormValues` reused wholesale, and never creates a `UserAccount`/login credential (task §22-23: Cabinet owner/TeamMember/UserAccount/Practitioner are never automatically conflated — confirmed via `TeamMember`'s own doc comment that a TeamMember can exist with no platform access at all). Récapitulatif shows every section read directly from the wizard's own accumulated state with per-section "Modifier" links back to that step (task §25) — no approved wireframe defines a review-before-completion pattern (Spec #9 Screen 07 goes straight to completion), so this is this task's own explicit addition, not a spec contradiction. Completion never claims a tenant/database was actually provisioned — the server notice is always shown (never dismissible), and the one "Découvrir mon espace (aperçu)" link to `/app` is explicitly labeled a non-persistent preview (task §26, RISK-020). `app/onboarding/layout.tsx` widened `max-w-lg` → `max-w-2xl` and switched from vertically-centered to top-aligned, mirroring UI-012ABCDE's own `book/layout.tsx` widening precedent. Back navigation never loses in-memory state (each step's data lives in the wizard's own component state, never unmounted, task §27); Continue always validates the current step first (task §28). FR/AR: full parity (2102 keys each, verified), RTL proven across both flows. No Laravel integration, no API calls, no database changes, no credential persistence, no tenant provisioning, no LocalStorage. 55 net new tests (10 in `validation.test.ts`, 7 in `login-page.test.tsx`, 6 each in `forgot-password-page.test.tsx`/`reset-password-page.test.tsx`, 5 in `onboarding-state.test.ts`, 15 in `onboarding-wizard.test.tsx`, 6 in `cross-onboarding-integrity.test.ts`) — 1968 total, up from the UI-013ABCDE baseline of 1913, typecheck/lint/build clean, backend regression unaffected (10/10, 26 assertions, no backend files touched) |
| UI-014 | COMPLETE | b85e04c | Final Frontend QA, UX Hardening & Prototype Freeze Checkpoint — a whole-product audit-and-repair pass across every completed surface (public, cabinet, platform-admin), not a feature-building task. **Gate 1 — Routes & Navigation**: all 68 registered routes verified 200 (production `next build` route table plus live dev-server checks); the 4 previously-regressed-then-fixed invalid-entity states (`/app/patients/[id]`, `/app/equipe/[id]`, `/app/stock/items/[id]`, `/admin/tenants/[id]`) re-verified via their existing component tests (135 tests across `patient-detail-page.test.tsx`/`team-member-detail-page.test.tsx`/`item-detail-page.test.tsx`/`tenant-detail-page.test.tsx`) rather than curl, since these are `"use client"` components whose real content only exists post-hydration — each shows a real "introuvable" `EmptyState`, never a crash; the `/app/[...slug]` catch-all confirmed to render a real bounded `EmptyState`, not a blank/broken page. The Paramètres ↔ Access Governance hierarchy (previously regressed once, UI-011X-FIX) re-verified intact via its own dedicated 22-test regression file (`parametres-access-hierarchy.test.tsx`) — `ParametresNav` and `AccessGovernanceNav` both render as separate levels on every Access sub-page. Main sidebar nav config (`lib/nav-config.ts`) confirmed to be exactly the 10 required items, no duplicates/omissions. **Gate 2 — Interactions**: a full dead-control sweep (empty `onClick`, `href="#"`, `TODO`/`FIXME`, `alert(`, stray `console.*`) found zero genuine bugs across the whole `frontend/src` tree; the topbar global "Créer" button — previously flagged dead in an earlier QA pass — is confirmed now WORKING (`quick-create-dialog.tsx`, 5 real links into existing creation flows). Every remaining bounded/no-op control (topbar notifications, Patient 360° "Facturer"/"Encaisser"/header "Plus", clinical/expense document download, "+Nouvelle facture"/"+Nouveau traitement") is confirmed an EXPLICIT FUTURE FEATURE via the established, consistent "Disponible dans une prochaine étape." Toast pattern, never a silent dead control. **Gate 3 — Runtime**: the previously-reported Finance "duplicate key `0`" warning (`expenses-page.tsx`/`ExpenseDetailDrawer`) is confirmed ALREADY FIXED — every list in that flow keys by a stable, unique fixture/sequence-generated string id (`expense-history-list.tsx`, `recent-activity-section.tsx`), no index/`Math.random()`/`Date.now()`-as-key pattern exists anywhere in that path; a broader tree-wide key-safety sweep found no genuine missing/unstable-key bug. **Gate 4 — Cross-Domain Integrity**: 7 of 8 audited relationships (Cabinet availability, Leave→Attendance, Finance, Inventory, Access Governance effective-access, Subscription→Entitlements, Platform Admin↔real tenant) confirmed single-sourced with existing `cross-*-integrity.test.ts` proof (11 such files exist). One genuine defect found and repaired: Agenda's own appointment-creation form (`appointment-form-dialog.tsx`) read a disconnected local `SERVICES` string array (`features/agenda/mock-data.ts`) instead of Paramètres' real `CabinetService[]` (`getCabinetServicesMockData()`) — inactive services (e.g. "Suivi", `svc-5`) remained bookable internally even though Public Booking correctly excluded them, and appointment duration was a free-typed value disconnected from the selected service's real `durationMinutes`. Fixed: `agenda-page.tsx` now sources `services={getCabinetServicesMockData()}`; `AppointmentFormDialog`'s service `<Select>` now offers only `active` services (while still showing an already-selected-but-now-inactive service on an existing appointment being edited, so editing `apt-10`/`apt-11` — both genuinely booked under "Suivi" before it was deactivated — never silently loses/corrupts that data); selecting a service now auto-fills `durationMinutes` from the matched `CabinetService`. `SchedulingFields`' quantized duration dropdown (`DURATION_OPTIONS`) widened from `[15,30,45,60]` to `[15,20,30,45,60]` so Contrôle/Suivi's real 20-minute duration is an exact, selectable option rather than silently falling back to the first option. One targeted regression test added (`agenda-page.test.tsx`) proving both the active-only filtering and the duration auto-fill. The previously-reported Attendance "everyone shows Repos, 0/0/0/0" state was traced end to end (business date → weekday resolution → schedule lookup → closure/leave → KPI aggregation, every step file:line-cited) and is **confirmed CORRECT, not a bug**: `MOCK_BUSINESS_DATE` (2026-08-23) is genuinely a Sunday, the mock schedule fixtures intentionally cover Monday-Saturday only, the weekday-reindex math is verified correct, and existing tests already prove both this rest-day case and a real Monday operational case render correctly — no code change made. **Gate 5 — UX/i18n/Responsive**: `fr.json`/`ar.json` key parity verified programmatically at exactly 2102/2102 leaf keys, zero mismatch; a hardcoded-string sweep across `features/`/`app/`/`components/` found no real French/English leakage bypassing `t()`; the global placeholder/prototype-boundary sweep (TODO/FIXME/"sera implémenté"/"pas encore implémenté"/etc.) found zero unexpected leftovers on any completed route — every hit is either a documented, consistent backend-boundary Toast/EmptyState pattern or dead, unreferenced TASK-003 `FoundationBadge`/`AreaPlaceholder` scaffolding (not user-reachable, left as-is, noted for future cleanup rather than touched in this QA-scoped task). No real browser-rendered visual QA tooling (Playwright/Cypress/Puppeteer) exists in this project — desktop/tablet/mobile/pixel-level checks were **not** performed and are honestly reported as such rather than claimed via curl/jsdom; RTL/i18n correctness instead relies on the large existing set of per-feature "renders in Arabic with RTL direction" component tests already in the suite. RISK-015 (Arabic PDF glyph corruption) re-confirmed still OPEN and safely gated (`isDocumentLanguageSupported`) — untouched, not attempted in this task. **Security audit**: zero `fetch`/`axios`/`/api/v1` calls, zero real `dangerouslySetInnerHTML`/`eval(`, zero `localStorage`/`IndexedDB` writes, and zero `process.env.*` usage beyond `NODE_ENV` anywhere in `features/`/`app/`/`components/` — every one of the 4 files mentioning `localStorage`/`IndexedDB` does so only in a doc comment disclaiming its use. 1 net new test (1969 total, up from the UI-013X baseline of 1968), full isolated regression clean (179 files, 0 failures), typecheck/lint/build clean, backend regression unaffected (10/10, 26 assertions, no backend files touched). No new business modules, no redesign, no backend integration, no specification rewrites — repair-only, exactly as scoped |

## Full-Stack Integration Sequence

The first tasks allowed to replace a frontend prototype seam with a real backend integration. Separate numbering from TASK-001–TASK-255 and from the UI Prototype Sequence above.

| Task | Status | Commit | Notes |
|------|--------|--------|-------|
| AUTH-001 | COMPLETE | 5b537ad | Real Authentication, Sessions & Password Recovery — first full-stack integration, replacing UI-013X's Login/Forgot/Reset prototype (RISK-019, resolved) with a real Laravel Identity module (`app/Modules/Identity/`) and a real frontend API boundary. **Backend**: `users`/`sessions`/`password_reset_tokens` migrations (UUIDv7 `users`, adapted `sessions` per DECISIONS.md ADR-020); `App\Modules\Identity\Infrastructure\Persistence\User` (bcrypt via Laravel's `hashed` cast, no plaintext/reversible storage anywhere); Laravel Sanctum stateful-SPA session-cookie authentication (not JWT/tokens, ADR-020) with `SESSION_DRIVER=database`; `POST /api/v1/auth/login` (`SessionGuard::attemptWhen()` so an inactive account's session is never created at all, two-layer rate limiting — per-IP route throttle + per-account+IP application-layer throttle), `POST /api/v1/auth/logout` (session invalidate + CSRF token rotation, `auth:sanctum`), `GET /api/v1/auth/me` (`auth:sanctum`), `POST /api/v1/auth/forgot-password`/`POST /api/v1/auth/reset-password` (Laravel's own `PasswordBroker` — random/hashed/single-use/time-limited token with zero bespoke crypto, `ResetPassword::createUrlUsing()` pointed at the Next.js route). Every failure mode collapses to one of three generic codes (`INVALID_CREDENTIALS`/`TOO_MANY_ATTEMPTS`/`INVALID_RESET_TOKEN`) — email-exists vs wrong-password, and unknown-email vs expired/used-token, are provably indistinguishable (dedicated tests assert identical status/body). A same-request container-singleton staleness bug affecting logout's own session bookkeeping was found and fixed during test-writing, not merely worked around (`Auth::forgetGuards()`/`app()->forgetInstance('auth.driver')` in `LogoutUser`, a `terminating()` cleanup in `AppServiceProvider` — full account in DECISIONS.md ADR-020 §7). A bounded `{error:{code,message,details,request_id}}` envelope (`App\Support\Http\ApiErrorResponse`, `App\Http\Middleware\AssignRequestId`) is implemented for these five endpoints only — the complete repository-wide version remains TASK-011/012's scope. **Frontend**: `src/lib/api-client.ts` (`apiFetch`/`ApiError`/`ApiUnavailableError` — the one shared `credentials:"include"` + CSRF-cookie fetch boundary every future `features/<module>/api.ts` builds on), `features/auth/api.ts`, `features/auth/session-context.tsx` (`SessionProvider`/`useSession`, scoped to `/app/*` only via `AuthGuard`/`src/app/app/layout.tsx` — `/auth`/`/book` untouched), `components/app/auth-guard.tsx` (loading/unauthenticated-redirect-with-`?from=`/unreachable-with-retry — frontend UX only, never claimed as the real security boundary). `LoginPage`/`ForgotPasswordPage`/`ResetPasswordPage` now call the real API with real loading/error states (invalid credentials, rate-limited, server-unavailable, invalid/expired reset token, missing-token — all new, real states the prototype never had); `AppShell`'s topbar "Compte" button and the Abonnement Blackout screen's "Se déconnecter" now call real `logout()`, replacing their previous future-feature Toast placeholders. No LocalStorage/sessionStorage/JWT anywhere (grep-confirmed) — the session cookie is HttpOnly, never read by JS. **Boundaries preserved, not falsely claimed**: tenant context/membership, permission enforcement, and onboarding tenant-provisioning (RISK-020, still open) remain entirely out of scope — `users` carries no role/tenant_id/employment field (CLAUDE.md §5); `/admin/*` remains deliberately unauthenticated (RISK-018, unchanged). **Testing**: 32 new backend feature tests (`tests/Feature/Identity/`, real PostgreSQL via `RefreshDatabase`) — 42/42 total (up from the 10/10 Phase 0 baseline), Pint clean. Frontend: 1995/1995 (up from the UI-014 baseline of 1969/179 files — 2 new test files, `src/lib/api-client.test.ts` and `src/components/app/auth-guard.test.tsx`), typecheck/lint/build clean. Additionally verified via a real Playwright-driven headless-Chromium run against the live dev backend+frontend+PostgreSQL (not mocked): wrong-password rejection, successful login, the real user-menu dialog, logout, the `/app` route-guard redirect, and the complete forgot-password → real emailed reset link (read from `storage/logs/laravel.log`, `MAIL_MAILER=log`) → reset → login-with-the-new-password loop — all confirmed working with zero browser console/page errors. |
